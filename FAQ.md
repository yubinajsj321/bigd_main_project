# 학생용 설치·세팅 & 문제 해결

이 문서는 **막혔을 때 찾아보는 문서**입니다. 처음 해보는 과정은 영상에서 시연으로 보여주고,
이 문서는 "그대로 따라 했는데 안 될 때" 증상으로 원인을 찾는 데 씁니다.

- **처음 할 때** → 그 장 영상을 보면서 따라 하세요.
- **막혔을 때** → 아래 [2. 증상으로 찾기](#2-증상으로-찾기)에서 내가 본 화면·오류 메시지를
  `Ctrl+F`로 검색하세요.
- 이번 주에 뭘 설치해야 하는지 궁금할 때 → [1. 장별 세팅](#1-장별-세팅)을 보세요.

---

## 0. 시작 전 준비물

### 텔레그램 — **2장 대면 수업 전까지** 설치해 두세요

**텔레그램 앱 설치 + 계정 생성.** 설치 방법은 **2장 영상**에서 안내합니다. 영상을 보면서
같이 하시면 되고, **늦어도 2장 대면 수업 전까지는 끝내 두세요.**

**대면 시간에 깔면 늦습니다.** 2장 대면은 **봇 만들기부터 시작**하는데, 그 자리에서 앱을
깔고 전화번호 인증을 받으면 시간이 많이 걸립니다. 인증 문자를 받아야 하니 **폰을 옆에 두고**
진행하세요.

> 1장에는 텔레그램을 쓰지 않습니다. 1장 준비물은 아래 표의 나머지 넷입니다.

### 준비물

| 준비물 | 설명 |
|---|---|
| Python **3.11 이상** | 코드가 `zoneinfo`(시간대 처리) 기능을 쓰기 때문에 3.9 미만에서는 동작하지 않습니다 |
| VS Code | 코드를 작성하고 터미널을 여는 편집기 |
| VS Code 확장 **Python** (Microsoft) | 오타·들여쓰기 오류를 미리 잡아주고, 파이썬 버전을 골라 확인할 수 있게 해 줍니다 ([설치 방법](#1장--개발-환경과-내-저장소-만들기)) |
| **Git** | VS Code가 내 코드를 GitHub에 올릴 때 뒤에서 씁니다. **직접 명령을 칠 일은 없습니다** ([확인 방법](#1장--개발-환경과-내-저장소-만들기)) |
| GitHub 계정 | **1장에 내 저장소를 만들고 매주 코드를 올립니다.** 10장부터는 자동 실행에도 씁니다 |
| 텔레그램 계정 | 봇을 만들고 메시지를 받는 계정. **2장 영상에서 안내하고, 2장 대면 전까지** 준비합니다 (1장에는 쓰지 않습니다) |

### 명령어 표기 안내

파이썬 실행 명령은 운영체제에 따라 다릅니다.

| | Windows | macOS |
|---|---|---|
| 파이썬 실행 | `python` | `python3` |
| 패키지 설치 | `pip` | `pip3` |

**이 문서에서는 `python`/`pip`으로 적습니다. macOS에서는 `python3`/`pip3`으로 바꿔 읽으세요.**

### 동작이 확인된 버전 조합

아래는 강사가 실제로 전 과정을 돌려본 버전입니다. **지금 잘 되던 것이 갑자기 안 될 때, 내가
설치한 버전을 이 표와 비교해 보세요.**

| | 확인된 버전 (2026-07-30) |
|---|---|
| Python | 3.11.15 |
| pandas | 3.0.3 |
| matplotlib | 3.11.1 |
| streamlit | 1.60.0 |
| holidays | 0.101 |
| requests | 2.34.2 |

`requirements.txt`에는 버전이 고정돼 있지 않습니다. 그래서 **설치하는 시점에 따라 위와 다른
버전이 깔릴 수 있고**, 라이브러리가 새 버전을 내면서 동작이 달라지는 경우가 드물게 있습니다.

지금 내 컴퓨터에 깔린 버전은 이렇게 확인합니다.

```
pip show pandas        # 특정 패키지 하나의 버전 확인   (macOS: pip3)
pip list               # 설치된 패키지 전체 목록      (macOS: pip3)
python --version       # 파이썬 버전 확인            (macOS: python3)
```

---

## 1. 장별 세팅

**설치하거나 계정을 만드는 세팅이 있는 장은 1·2·8·10·12주 다섯 번뿐입니다.**
**3~7주, 9주, 11주, 13주에는 새로 세팅할 것이 없습니다.** 매주 뭔가 깔아야 하는 것이 아니니
안심하고 코드에 집중하세요.

### 1장 — 개발 환경과 내 저장소 만들기

**해야 할 것**

1. Python 3.11 이상이 깔려 있는지 확인합니다.
   ```
   python --version          # macOS: python3 --version
   ```
2. VS Code를 설치합니다.
3. **VS Code에 `Python` 확장을 설치합니다.**
   - 왼쪽 확장 탭(**Windows** `Ctrl+Shift+X` / **macOS** `⇧⌘X`)에서 `Python`을 검색합니다.
   - 검색 결과에 이름이 비슷한 것이 여럿 나옵니다. **게시자가 Microsoft인 것**을 고르세요.
   - 이 하나만 깔면 **Pylance**(자동완성·문법 검사)와 **Python Debugger**가 함께 설치됩니다.
     따로 찾아서 깔 필요가 없습니다.
   - 확장이 없어도 터미널에서 코드를 실행하는 것 자체는 됩니다. 다만 오타나 들여쓰기 오류를
     **실행하기 전에** 빨간 밑줄로 알려주고, 파이썬이 여러 버전 깔려 있을 때 오른쪽 아래에서
     지금 쓰는 버전을 확인·변경할 수 있어서 훨씬 편합니다.

   > **⚠️ `Code Runner` 확장은 설치하지 마세요.**
   > "파이썬 실행에 편하다"고 많이 추천되지만 이 수업에서는 문제를 만듭니다. Code Runner는
   > 파일이 있는 폴더가 아닌 다른 위치를 기준으로 실행해서, **2장에 만들 `.env` 파일을 찾지
   > 못해** 토큰이 `None`으로 나옵니다. 분명히 값을 넣었는데 안 되는 것처럼 보여서 원인을 찾기
   > 아주 어렵습니다. 이 수업에서는 **항상 터미널에서 `python 파일이름.py`로 실행**하세요.

4. **Git이 깔려 있는지 확인합니다.** 터미널에서 아래를 실행해 버전 숫자가 나오면 된 것입니다.
   ```
   git --version
   ```
   - **없다고 나오면** — **Windows**는 https://git-scm.com 에서 받아 **전부 기본값으로**
     설치합니다. **macOS**는 위 명령을 친 순간 설치 안내 창이 뜨므로 그대로 설치하면 됩니다.
   - **`git` 명령을 직접 칠 일은 바로 아래 5-1번 한 번뿐입니다.** 그 뒤로는 13주 내내 VS Code가
     뒤에서 대신 쓰기 때문에 깔려 있기만 하면 됩니다. 없으면 8번의 버튼이 아예 나타나지 않습니다.
   - 설치했다면 **VS Code를 껐다가 다시 켜세요.** 켜져 있는 동안 깐 것은 인식되지 않습니다.
5. GitHub 계정을 만듭니다.
5-1. **Git에게 내가 누구인지 알려줍니다 — 이 컴퓨터에서 한 번만 하면 됩니다.**
   커밋에는 **누가 했는지**가 함께 기록되기 때문에 이름과 이메일이 필요합니다.
   먼저 아래 한 줄만 쳐 보세요. **값이 나오면 이미 설정된 것이니 6번으로 넘어가면 됩니다.**
   ```
   git config --global user.name
   ```
   아무것도 안 나오면 아직 설정되지 않은 것입니다. 아래 두 줄로 설정합니다.
   ```
   git config --global user.name "내_깃허브_아이디"
   git config --global user.email "내_이메일@example.com"
   ```
   - ⚠️ **`내_깃허브_아이디`와 이메일은 각자 자기 것을 넣습니다.** 화면에 보이는 대로 복사해
     붙이면 안 됩니다.
   - ⚠️ **이메일은 5번에서 만든 GitHub 계정 이메일과 같게 씁니다.** 다르면 커밋이 내 것으로
     잡히지 않습니다.
   - **이걸 안 하면 8번에서 막힙니다** — `Author identity unknown` / `Please tell me who you are`
     라고 뜨는데, **원인이 화면에 드러나지 않아** 찾기 어렵습니다. 그래서 미리 해 둡니다.
   - `--global` 이라서 **이 컴퓨터의 모든 프로젝트에 한 번에 적용**됩니다. 학기 중 다시 할 일은
     없습니다.
6. **폴더를 두 개 만듭니다 — 「메인 폴더」와 「장별 폴더」입니다.**

   | 폴더 | 하는 일 | 13주 동안 |
   |---|---|---|
   | **메인 폴더** (예: `bigd_project`) | 내가 실제로 코드를 실행하는 곳. GitHub에 연결되는 것도 **이 폴더 하나뿐**입니다 | 계속 씁니다 |
   | **장별 폴더** (예: `bigd_01_setup`) | 매주 받은 압축 파일을 푸는 곳. 받은 원본 그대로 보관합니다 | 매주 하나씩 늘어납니다 |

   매주 하는 일은 **장별 폴더에 압축을 풀고 → 그 안의 파일을 메인 폴더에 덮어쓰는 것**입니다.
   받은 원본이 장별 폴더에 그대로 남아 있으므로, 메인 폴더에서 뭔가 잘못돼도 언제든 다시
   가져올 수 있습니다.

   > **⚠️ 메인 폴더 이름은 영문·숫자로 지으세요** (`bigd_project`, `stock2026` 등).
   > 8번에서 이 폴더 이름이 **GitHub 저장소 이름으로 그대로 제안**되는데, GitHub 저장소
   > 이름에는 한글을 쓸 수 없어서 **한글 한 글자당 하이픈 하나로 바뀝니다.**
   > `빅데이터프로젝트` → `----------` 이 되는 식입니다. 그런데 **입력칸에는 원래 한글
   > 이름이 그대로 보여서** 자기 저장소 이름이 그렇게 됐다는 걸 알아채기 어렵습니다
   > → [2절 24번](#24-저장소-이름이-하이픈-범벅이-됐습니다)

7. **이번 주 파일을 메인 폴더에 넣고, `.gitignore`를 직접 만듭니다.**
   - 받은 압축 파일을 **장별 폴더**에 풀고, 그 안의 **`watchlist.csv`** 와
     **`01-WHATS-NEW.md`** 를 **메인 폴더**로 복사합니다.
     (`FAQ.md`는 읽는 문서이니 장별 폴더에 그대로 두어도 됩니다.)
   - VS Code에서 **File → Open Folder**로 **메인 폴더**를 엽니다.
   - **처음 열면 "이 폴더의 파일을 신뢰합니까" 화면이 뜹니다. 반드시 신뢰(Trust)를 고르세요.**
     제한 모드로 두면 뒤에 쓸 버튼이 **아예 나타나지 않습니다**
     → [2절 22번](#22-소스-제어-패널에-publish-to-github-버튼이-없습니다)
   - 왼쪽 탐색기에서 **새 파일** 아이콘을 누르고 이름을 **`.gitignore`** 로 입력합니다
     (**맨 앞의 점까지** 정확히).
   - 아래 내용을 그대로 적고 저장합니다.
     ```
     # 비밀정보 — 절대 커밋하지 말 것
     .env
     .env.*
     !.env.example
     *token*.txt
     *secret*

     __pycache__/
     *.pyc
     *.docx
     *.pptx
     *.pdf
     *.jpg
     *.jpeg
     *.png
     ```

   > **왜 받은 파일에서 복사하지 않고 직접 만드나요?**
   > **이 파일이 무엇을 막고 있는지 알고 넘어가야 하기 때문**입니다. `.gitignore`는 **틀려도
   > 아무 오류를 내지 않는 파일**입니다. 잘못 적혀 있거나 아예 없어도 그 자리에서는 조용하고,
   > 몇 주가 지나서야 2장에 만든 `.env`(토큰이 들어 있습니다)가 그대로 GitHub에 올라간 걸
   > 알게 됩니다. 그때는 이미 늦습니다 → [2절 18번](#18-토큰을-실수로-노출했을-때)
   > 한 번 직접 쳐 보면 어떤 파일이 목록에 들어 있는지 눈으로 확인하게 됩니다.

   > **📁 이 파일이 보이나요? — 「숨김」의 기준이 맥과 윈도우에서 다릅니다.**
   >
   > | | 무엇을 숨기나 | `.gitignore`는 |
   > |---|---|---|
   > | **macOS · 리눅스** | **이름이 점(`.`)으로 시작하면** 숨깁니다 (이름 규칙) | **안 보입니다.** `⌘⇧.` 를 누르면 보입니다 |
   > | **Windows** | **hidden 속성이 붙은 것만** 숨깁니다. 점은 상관없습니다 (파일 속성) | **그냥 보입니다.** 전체 선택에도 딸려 옵니다 |
   >
   > 그래서 맥에서는 `⌘A`(전체 선택)로 복사할 때 이 파일이 **조용히 빠지고**, 윈도우에서는
   > 빠지지 않습니다. **어느 쪽이든 직접 만드는 것은 같습니다** — 위에서 말한 이유 때문입니다.
   >
   > 반대로 **`.git` 폴더는 윈도우에서도 안 보입니다.** 이름 때문이 아니라 Git이 만들면서
   > hidden 속성을 붙이기 때문입니다. 탐색기 **보기 → 숨긴 항목**을 켜야 보입니다.

8. **저장소 만들기 — VS Code의 `Publish to GitHub` 버튼 하나로 끝납니다.**

   저장소 생성 · 첫 커밋 · GitHub 업로드가 **한 번에** 이루어집니다.

   1. 왼쪽 **소스 제어**(가지 모양) 아이콘을 누릅니다
      (**Windows** `Ctrl+Shift+G` / **macOS** `⌃⇧G`).
   2. 버튼이 두 개 보입니다. **아래쪽 `Publish to GitHub`을 누르세요.**
      - ⚠️ **위쪽 `Initialize Repository`가 아닙니다.** 그것을 누르면 아래 단계들이 전부
        건너뛰어지고 저장소도 만들어지지 않습니다
        → [2절 26번](#26-initialize-repository를-눌러-버렸습니다)
   3. **GitHub 로그인 창이 뜹니다. 안내에 따라 로그인하고 승인합니다.**
      - **Windows**는 사용자명·비밀번호를 묻는 **`Connect to GitHub`** 창이 먼저 뜹니다.
        입력하고 **`Continue`** 를 누르세요.
      - 열린 브라우저에서 승인을 요구하면 **그 화면의 안내에 따라 승인**합니다.
      - **⚠️ Windows는 로그인 창이 두 번 뜹니다. 두 번째는 고장이 아닙니다** — 아래 5번
        다음에 한 번 더 나옵니다 → [2절 27번](#27-로그인-창이-두-번-뜹니다)
   4. 저장소 이름을 확인합니다. 메인 폴더 이름이 그대로 제안됩니다 (6번의 ⚠️ 참고).
   5. **`public`을 고릅니다.**
      - ⚠️ **위쪽에 `private`이 미리 선택돼 있습니다.** 그냥 엔터를 치면 private이 됩니다.
        **아래 `public` 항목을 마우스로 클릭**하세요.
      - private으로 만들어도 수업은 그대로 진행됩니다. 다만 12장에 한 단계가 더 있습니다
        (아래 **「꼭 알아둘 것」** 참고). **나중에 바꿀 수 있습니다.**
   6. **(Windows만) 로그인 창이 한 번 더 뜹니다.** **`Sign in with your browser`** 라고
      적힌 창입니다. 눌러서 브라우저 안내에 따라 승인하세요.
      - **"아까 로그인했는데 왜 또?"** — 하는 일이 다른 두 번입니다. 첫 번째는 **저장소를
        만들기 위한** 로그인이고, 두 번째는 **파일을 올리기(push) 위한** 로그인입니다.
        Windows는 이 둘을 서로 다른 프로그램이 처리해서 각각 묻습니다.
      - **macOS는 이 창이 없습니다.** VS Code가 두 가지를 모두 처리해서 로그인이 한 번뿐입니다.
      - **둘 다 한 번만 하면 됩니다.** 이후 13주 동안 다시 묻지 않습니다.
   7. 끝나면 오른쪽 아래에 알림이 뜹니다. **브라우저에서 내 GitHub 저장소를 열어 파일이
      올라갔는지 눈으로 확인**하세요.

9. 라이브러리를 설치합니다. **여기서 한 번에 전부 깔면 13주 동안 다시 깔 일이 없습니다.**
   ```
   pip install requests python-dotenv pandas matplotlib holidays streamlit libsql-client
   ```
   macOS는 `pip` 대신 `pip3`을 씁니다.

   > **왜 `pip install -r requirements.txt`가 아닌가요?**
   > `requirements.txt`는 *"이 프로젝트가 지금 필요로 하는 것"* 을 적어 둔 목록인데, 이 목록은
   > **장마다 자랍니다**(2장에 2줄로 생겨서 12장에 7줄이 됩니다). 지금 그 파일을 읽어
   > 설치하면 앞으로 필요한 것이 빠집니다. 그래서 1장에만 이름을 직접 적어 한 번에 깝니다.
   > **"목록에 적는 것"과 "실제로 설치하는 것"은 별개**라는 점을 기억해 두세요 — 10장에
   > GitHub이 여러분 대신 `requirements.txt`를 읽어 설치하게 될 때 이 이야기가 다시 나옵니다.

**꼭 알아둘 것**

**2장부터는 매주 이 두 버튼만 누르면 됩니다.** 오늘 한 번 연결해 두었으므로, 앞으로는
저장소를 다시 만들 일도 로그인할 일도 없습니다.

| 순서 | 하는 일 |
|---|---|
| ① | 그 주 압축 파일을 **장별 폴더**에 풀고, 그 파일들을 **메인 폴더에 덮어쓰기** |
| ② | VS Code **소스 제어** 패널을 엽니다 — 바뀐 파일 목록이 떠 있습니다 |
| ③ | **맨 위 한 줄짜리 입력칸**(`Message …`)에 **무엇을 바꿨는지 먼저 적습니다** |
| ④ | 그 아래 파란 **`Commit`** 버튼을 누릅니다 |
| ⑤ | 버튼이 **`Sync Changes`** 로 바뀝니다. 그걸 누르면 GitHub에 올라갑니다 (확인 창이 한 번 뜹니다 — `OK`) |

> **⚠️ ③을 건너뛰고 `Commit`부터 누르지 마세요.** 메시지 없이 누르면 `COMMIT_EDITMSG`라는
> 낯선 파일이 열리면서 소스 제어 패널이 먹통이 된 것처럼 보입니다. 빠져나오는 방법은
> → [2절 23번](#23-commit을-눌렀더니-낯선-편집기가-열리고-패널이-잠겼습니다)

> **입력칸이 눈에 잘 안 띕니다.** 소스 제어 패널 **맨 위의 한 줄**입니다
> (`Message (⌘Enter to commit on 'main')` 이라고 흐리게 적혀 있습니다). 그 바로 아래가
> 파란 `Commit` 버튼입니다.

**터미널에 `git` 명령을 칠 일은 13주 내내 없습니다.** 전부 이 버튼들로 합니다.

**저장소를 public으로 만들까요, private으로 만들까요?** (위 8번 5)에서 고르는 그 항목입니다)

public은 누구나 내 코드를 볼 수 있고, private은 나만 볼 수 있습니다.

- **둘 다 됩니다.** 10장부터 쓰는 자동 실행(GitHub Actions)은 private 저장소에서도 무료
  사용량(월 2,000분)이 주어지는데, 이 수업이 쓰는 분량은 **월 100분 정도**입니다. 그래서
  private을 골라도 요금이 청구될 걱정은 없습니다.
- **다만 private을 고르면 12장에 한 단계가 더 있습니다.** 12장에 대시보드를 인터넷에
  배포할 때(Streamlit Community Cloud), private 저장소는 **"이 저장소를 봐도 된다"는 권한을
  따로 승인**해 줘야 배포 화면의 저장소 목록에 나타납니다. 어려운 작업은 아니지만, 그런
  단계가 있다는 걸 모른 채 12장에 처음 마주치면 저장소가 사라진 줄 알고 한참 헤매게 됩니다
  → [2절 16번](#2-증상으로-찾기)
- **public으로 만든다면** `.env`를 실수로 올렸을 때 누구나 내 토큰을 볼 수 있으므로
  `.gitignore` 확인이 훨씬 더 중요해집니다.

**지금 고른 것은 나중에 바꿀 수 있습니다.** (저장소 Settings 맨 아래에서 전환)

**흔히 막히는 것** → [2절 11번](#2-증상으로-찾기) (`python`을 못 찾는다고 나올 때) /
[2절 22번](#22-소스-제어-패널에-publish-to-github-버튼이-없습니다) `Publish to GitHub` 버튼이 안 보임 /
[2절 23번](#23-commit을-눌렀더니-낯선-편집기가-열리고-패널이-잠겼습니다) 낯선 편집기가 열리고 패널이 잠김 /
[2절 24번](#24-저장소-이름이-하이픈-범벅이-됐습니다) 저장소 이름이 하이픈 범벅 /
[2절 25번](#25-publish-to-github을-눌렀는데-아무-일-없이-처음-화면으로-돌아갑니다) 눌러도 아무 일이 없음

### 2장 — 텔레그램 봇과 `.env`

**해야 할 것**

1. `.env` 파일을 만듭니다. **`.env.example`을 복사해 이름만 `.env`로 바꾸는 방법이 가장
   안전합니다.**
   - **봇보다 이것을 먼저 만듭니다.** 토큰은 발급되는 순간 화면에 한 번 뜨고 마는데, **받아
     적을 곳이 미리 있어야** 다시 찾아 헤매지 않습니다.
2. **`.gitignore`가 `.env`를 막아주는지 눈으로 확인합니다.** 1장에 직접 만든 그 파일입니다.
   - `.env`를 만든 뒤 VS Code **소스 제어** 패널을 열어 보세요. 방금 만든 `.env`는
     **목록에 뜨지 않습니다.** 다른 새 파일은 뜨는데 `.env`만 없습니다.
   - **그게 정상이고, 그것이 `.gitignore`가 하는 일입니다.** 목록에 없으니 `Commit`을 눌러도
     올라가지 않고, 토큰이 GitHub에 새어 나갈 길이 막힙니다.
   - ⚠️ **반대로 `.env`가 목록에 보인다면 `.gitignore`가 없거나 잘못된 것입니다.** 그대로
     `Commit` → `Sync`를 누르면 토큰이 GitHub에 올라갑니다. 1장 7번의 내용대로
     `.gitignore`가 메인 폴더에 있는지 먼저 확인하세요
     → [1절 1장](#1장--개발-환경과-내-저장소-만들기) 7번
   - 💡 **지금은 `.env`에 아직 진짜 토큰이 없습니다.** 그래서 여기서 확인이 어긋나더라도
     잃을 것이 없습니다 — **막히는지 먼저 보고, 그다음에 토큰을 넣는 순서입니다.**
3. 텔레그램 앱에서 `@BotFather`를 검색해 대화를 시작하고 `/newbot`을 입력합니다.
4. 봇 이름과 **username**을 정합니다.
   - **`@BotFather`는 이름을 두 번 묻습니다.** 먼저 **표시 이름(name)** — 대화창 위에 보이는
     이름이라 한글도 되고 겹쳐도 되며 나중에 바꿀 수 있습니다. 그다음이 **username** —
     `@…`로 부르는 주소이고 규칙이 까다롭습니다. **둘은 다른 것입니다.**
   - **username은 전 세계에서 겹치면 안 되고, 반드시 `bot`으로 끝나야 합니다.** 흔한 이름
     (`stock_bot` 등)은 이미 누군가 쓰고 있어서 거절됩니다. **여러 번 다시 시도하게 되는 것이
     정상**이니 당황하지 마세요.
   - **학번이나 이름을 넣어 고유하게** 만드세요. 예: `stock_2026001_bot`
5. 발급된 **토큰**을 복사해 **1번에서 만들어 둔 `.env`의 `TELEGRAM_BOT_TOKEN=` 뒤에 바로
   붙여넣습니다.** 나중에 다시 보기 번거로우니 미루지 마세요.
6. **내가 만든 봇에게 아무 메시지나 하나 보냅니다.** ← 다음 단계 전에 **반드시** 해야 합니다.
7. `python telebot.py`를 실행하고, 출력된 내용에서 `chat`의 `id` 값을 찾습니다. 이 숫자가
   `TELEGRAM_CHAT_ID`입니다. **찾았으면 `.env`의 `TELEGRAM_CHAT_ID=` 뒤에 적습니다.**
   - `telebot.py`는 `.env`의 토큰을 읽어 실행됩니다. **5번을 건너뛰었다면 여기서 막힙니다.**

**흔히 막히는 것** → [2절 5번](#2-증상으로-찾기) 봇 username 거절 /
[2절 6번](#2-증상으로-찾기) `getUpdates`가 빈 결과 /
[2절 7번](#2-증상으로-찾기) `.env`가 `.env.txt`로 저장됨

### 3~7장 — 세팅 없음

새로 설치하거나 계정을 만들 것이 없습니다. 코드에만 집중하세요.

#### ⚠️ 다만 7장에 파일 하나가 압축에서 빠집니다 — `watchlist.csv`

**7장부터 압축에는 `watchlist.csv`가 들어 있지 않습니다.** 대신 **`watchlist.example.csv`**
(본보기)가 들어 있습니다. 내용은 같고 이름만 다릅니다.

**왜 그런가** — 여러분은 매주 압축을 풀어 **내용을 전부 프로젝트 폴더에 덮어씁니다.** 그런데
6장에 `watchlist.csv`에 **자기 종목을 넣었죠.** 7장 압축에 그 파일이 들어 있으면 그
편집이 **기본 3종목으로 되돌아갑니다.** 게다가 되돌아간 줄 모르고 커밋하면 GitHub에 있는
것까지 되돌아가고, 10장 이후에는 **자동 알림이 남의 종목으로 옵니다.**

`.env`와 똑같은 방식입니다 — 압축에는 `.env.example`만 들어 있고 실제 `.env`는 여러분이
만들어 갖고 있죠. **내가 채운 파일은 배포물이 건드리지 않습니다.**

**해야 할 것은 상황에 따라 다릅니다.**

| 내 상황 | 할 일 |
|---|---|
| 6장에 쓰던 `watchlist.csv`가 **그대로 있다** | **아무것도 안 해도 됩니다.** `watchlist.example.csv`는 무시하세요 (지워도 됩니다) |
| 폴더를 잃어버려 `watchlist.csv`가 **없다** | `watchlist.example.csv`를 **복사한 뒤 이름을 `watchlist.csv`로 바꾸세요.** 그다음 내 종목으로 고칩니다 |

> **이름을 바꾸는 것이지 새로 만드는 것이 아닙니다.** 확장자 `.csv`는 그대로 두고 가운데
> `.example`만 지웁니다. 원본을 지우지 말고 **복사본**을 만들어 바꾸는 편이 안전합니다.
>
> ⚠️ 윈도우 탐색기에서 확장자가 숨겨져 있으면 화면에 `watchlist.example`까지만 보입니다.
> 그 상태로 `watchlist`로 고치면 실제 이름이 `watchlist.csv`가 됩니다(원하던 결과입니다).
> **보기 → 파일 확장명**을 켜 두고 하면 헷갈리지 않습니다.

파일 이름을 바꾸는 대신 새 파일을 만들어도 됩니다. 내용은 이 네 줄입니다(마지막 줄 뒤에서
엔터를 한 번 눌러 두세요 — 이유는 [2절 8-1번](#8-1-watchlistcsv를-고칠-때--엑셀로-열지-마세요)).

```
code,name
005930,삼성전자
000660,SK하이닉스
005380,현대자동차
```

**8장 이후에도 계속 이렇습니다.** 앞으로 압축에는 늘 본보기만 들어 있습니다.
→ 증상별 안내: [2절 8번](#2-증상으로-찾기)

### 8장 — Turso DB 만들기 (웹 콘솔)

과거 종가를 쌓아둘 클라우드 DB를 각자 하나씩 만듭니다. 텔레그램 봇 토큰과 마찬가지로 **이
부분은 자동화가 안 되고 각자 직접 발급받아야 합니다.**

**해야 할 것**

1. 브라우저에서 https://turso.tech 에 접속해 **GitHub 계정으로 로그인**합니다.
2. **Create Database**를 누르고, 이름은 `telebot-stock`, 지역(region)은 기본값 그대로 둡니다.
   - **DB 이름은 내 계정 안에서 겹치면 안 됩니다.** (텔레그램 봇 username처럼 전 세계에서
     유일할 필요는 없고, 내 계정 안에서만 유일하면 됩니다.) 이미 같은 이름을 쓰고 있다면
     거절되니 뒤에 숫자를 붙이는 식으로 바꾸세요. 예: `telebot-stock-2`
3. **DB를 만들면 나오는 화면에 Database URL이 보입니다.** 그것을 복사합니다.
   → 이 값이 `TURSO_DATABASE_URL`입니다. (`libsql://...` 또는 `https://...` 형태이며 둘 다
   그대로 넣으면 됩니다.)
4. 같은 화면에서 **Create Token**을 누릅니다. 발급된 값이 `TURSO_AUTH_TOKEN`입니다.
5. `.env`에 두 줄을 추가합니다.
   ```
   TURSO_DATABASE_URL=3번에서_확인한_URL
   TURSO_AUTH_TOKEN=4번에서_발급받은_토큰
   ```

**CLI를 설치할 필요가 없습니다.** 웹 콘솔에서 발급한 값만으로 이 프로젝트가 전부 정상
동작하는 것을 2026-07-30 Windows에서 확인했습니다. 인터넷을 검색하면 `turso auth signup`
같은 명령어로 설치·로그인하는 안내가 많이 나오지만 **이 수업에서는 쓰지 않습니다.**

> ⚠️ **발급된 토큰을 카톡·디스코드·과제 제출물·AI 채팅 등 어디에도 붙여넣지 마세요.**
> 복사한 즉시 `.env`에만 넣으세요. **편집기에서 `.env`를 선택해 둔 채로 AI 도구를 쓰면 붙여넣지
> 않아도 전달될 수 있습니다.** 이미 노출된 것 같다면 → [2절 18번](#18-토큰을-실수로-노출했을-때)

**흔히 막히는 것** → [2절 12번](#2-증상으로-찾기) (Turso 읽기/저장 실패)

### 9장 — 세팅 없음

새로 설치하거나 계정을 만들 것이 없습니다.

### 10장 — GitHub Secrets 등록

> **이번 주에 새로 설정할 것은 Secrets 네 개뿐입니다.** 내 코드를 GitHub에 올리는 일은
> 1장에 연결해 두고 **9주 동안 매주 해 온 그 `Commit` → `Sync Changes`** 그대로입니다.
> 이번 주 워크플로 파일 두 개도 **VS Code에서 만들어 늘 하던 두 버튼으로 올리므로**,
> 여기서 새로 배우는 git 조작은 없습니다. **1장에 `.gitignore` 를 만든 것과 같은
> 방법입니다** — 새 파일 이름 칸에 `.github/workflows/notify.yml` 을 통째로 치면
> 폴더까지 한 번에 만들어집니다.

**해야 할 것**

1. **먼저 내 저장소에 최신 코드가 올라가 있는지 확인합니다.**
   GitHub Actions는 **GitHub이 내 저장소의 코드를 읽어 실행**하는 것이라, 내 컴퓨터에만 있는
   코드는 실행되지 않습니다. 브라우저에서 내 저장소를 열어 `notify_stock_price.py` ·
   `stock_utils.py` · `requirements.txt` · `watchlist.csv`가 **이번 주 것으로** 보이는지
   확인하세요. 안 보이면 VS Code 소스 제어 패널에서 `Commit` → `Sync Changes`를 한 번 더
   합니다 (1장에서 익힌 그대로입니다).

   > ⚠️ **같이 확인할 것 하나 — 저장소 화면 왼쪽 위의 브랜치 이름이 `main`인가요?**
   > `master`로 보인다면 **Settings → Branches**에서 `main`으로 바꾸세요. 자동 실행은
   > **기본 브랜치만** 보기 때문에, 이대로 두면 오류 하나 없이 **영영 실행되지 않습니다**
   > → [2절 26번](#26-initialize-repository를-눌러-버렸습니다)

2. 내 저장소에서 **Settings → Secrets and variables → Actions → New repository secret**으로
   아래 4개를 등록합니다.

   | 이름 | 값 |
   |---|---|
   | `TELEGRAM_BOT_TOKEN` | `.env`의 같은 값 |
   | `TELEGRAM_CHAT_ID` | `.env`의 같은 값 |
   | `TURSO_DATABASE_URL` | `.env`의 같은 값 |
   | `TURSO_AUTH_TOKEN` | `.env`의 같은 값 |

   - **`.env`와는 별개입니다.** `.env`는 **내 컴퓨터**에서 실행할 때 쓰는 파일이고, Secrets는
     **GitHub 서버**가 대신 실행할 때 쓰는 값입니다. 같은 값을 양쪽에 각각 넣어야 합니다.
   - 등록하고 나면 값은 다시 볼 수 없고 이름만 보입니다. 정상입니다.
3. Actions 탭에서 **Run workflow** 버튼으로 직접 실행해 결과를 확인합니다. 로그에 `✅`/`🎉`가
   뜨면 4개가 모두 제대로 등록된 것입니다.

**저장소를 public으로 바꿔야 하나요?**

> **public이 필수는 아닙니다.** 저장소는 private으로 두어도 자동 실행이 그대로 됩니다. 이
> 수업에서 쓰는 자동 실행 분량은 무료 한도(월 2,000분)의 5% 정도라 요금 걱정도 없습니다.
> 고르는 기준은 [1장의 public/private 안내](#1장--개발-환경과-내-저장소-만들기)에 정리해
> 두었습니다.
>
> 다만 **12장에 대시보드를 배포**할 때 저장소가 private이면 Streamlit에 저장소 접근 권한을
> 따로 승인해 줘야 합니다 → [2절 16번](#2-증상으로-찾기). 그게 번거로우면 그때 public으로
> 바꿔도 됩니다.
>
> **public으로 두는 경우에는** `.env`를 실수로 올리면 누구나 내 토큰을 볼 수 있으니
> `.gitignore` 확인이 더 중요해집니다.

**흔히 막히는 것** → [2절 14번](#2-증상으로-찾기) 스케줄이 안 돎 /
[2절 15번](#2-증상으로-찾기) 빨간 ✗ / [2절 2번](#2-증상으로-찾기) 원인 해석표

### 11장 — 세팅 없음

새로 설치하거나 계정을 만들 것이 없습니다. 다만 이 장에는 실습으로 Secrets를 일부러 지웠다가
되돌립니다. **수업이 끝나기 전에 반드시 원래대로 복구하고 나가세요.** 복구를 잊으면 12·13장에
원인을 알 수 없는 실패를 겪게 됩니다.

### 12장 — Streamlit 대시보드 배포

지금까지 만든 데이터를 **웹페이지로도 볼 수 있게** 인터넷에 배포합니다.

**해야 할 것**

1. https://share.streamlit.io 에 접속해 **GitHub 계정으로 로그인**합니다.
2. **New app**을 누르고 다음을 지정합니다.

   | 항목 | 값 |
   |---|---|
   | Repository | 내 저장소 |
   | Branch | `main` |
   | Main file path | `dashboard.py` |

3. **Advanced settings → Secrets**에 4개 값을 등록합니다.
   - **⚠️ 10장에 등록한 GitHub Secrets와는 또 다른 곳입니다.** 같은 값을 **세 번째로** 넣는
     셈입니다.

     | 어디에 | 무엇이 쓰나 |
     |---|---|
     | `.env` | 내 컴퓨터에서 직접 실행할 때 |
     | GitHub Secrets | GitHub Actions 자동 실행이 돌 때 |
     | Streamlit Secrets | 배포된 대시보드가 돌 때 |

   - 형식은 TOML이라 등호와 따옴표를 씁니다. `.env`에는 따옴표를 안 쓰는데 **여기는 써야
     한다**는 점이 다릅니다.
     ```
     TELEGRAM_BOT_TOKEN = "값"
     TELEGRAM_CHAT_ID = "값"
     TURSO_DATABASE_URL = "값"
     TURSO_AUTH_TOKEN = "값"
     ```
   - **⚠️ 따옴표는 반드시 곧은 따옴표(`"`)여야 합니다.** 메모장·워드·메신저를 거쳐 붙여넣으면
     둥근 따옴표(`“ ”`)로 자동 변환되는 경우가 있는데, 그러면 **오류 메시지 없이 `Save` 버튼이
     비활성 상태로 남습니다.** 저장이 안 되는데 이유가 안 보인다면 **따옴표만 지우고 직접 다시
     입력**해 보세요.
4. **Deploy**를 누르고 빌드가 끝날 때까지 기다립니다. 처음 배포는 몇 분 걸릴 수 있습니다.

배포한 뒤에는 `main` 브랜치에 push하면 보통 몇십 초~1분 안에 자동으로 다시 배포됩니다.

> **Secrets를 Deploy 이후에 넣거나 고쳤다면 앱을 재시작(Reboot)해야 반영됩니다.**
> 값을 저장해도 화면이 그대로라면 이 경우입니다 → [2절 17번](#2-증상으로-찾기)

**흔히 막히는 것** → [2절 16번](#2-증상으로-찾기) private 저장소가 목록에 없음 /
[2절 17번](#2-증상으로-찾기) 한글 깨짐·Secrets 반영 안 됨·대시보드에서만 조회 실패

### 13장 — 세팅 없음

새로 설치하거나 계정을 만들 것이 없습니다.

### 부록 — 터미널에서 `git push`를 쓰고 싶어질 때

> **이 수업에서는 필요 없습니다.** 13주 내내 VS Code의 `Commit` → `Sync Changes` 두 버튼으로
> 끝나고, 터미널에 `git` 명령을 칠 일은 한 번도 없습니다. 아래는 **수업이 끝난 뒤** 다른
> 도구나 다른 컴퓨터에서 GitHub을 쓰게 될 때를 위한 참고입니다.

GitHub은 **계정 비밀번호로는 push를 받아주지 않습니다.** 터미널에서 직접 `git push`를 하려면
아래 둘 중 하나를 설정해야 합니다. 무엇을 고르든 결과는 같습니다.

**PAT — 토큰을 비밀번호 대신 씁니다**

1. GitHub 오른쪽 위 프로필 → **Settings**(저장소 Settings가 아니라 **계정** Settings입니다)
2. 맨 아래 **Developer settings** → **Personal access tokens** → **Tokens (classic)**
3. **Generate new token (classic)** → 이름을 적고, 권한에서 **`repo`** 를 체크
4. 만료 기간을 정하고 생성 → **발급된 문자열을 복사합니다.**
5. 터미널에서 `git push` 하면 아이디와 비밀번호를 묻는데, **비밀번호 자리에 이 토큰을
   붙여넣습니다.**

> ⚠️ **토큰은 이 화면을 벗어나면 다시 볼 수 없습니다.** 잃어버리면 새로 만들어야 합니다.
> **기본 만료가 30일**이라 한 달 뒤 갑자기 push가 거부되는 일이 흔합니다.
> 그리고 이것도 비밀입니다 — `.env`의 토큰들과 똑같이 취급하세요
> ([2절 18번](#18-토큰을-실수로-노출했을-때)).

**SSH 키 — 열쇠 한 쌍을 만듭니다**

1. 터미널에서 키를 만듭니다(물어보는 것은 전부 엔터로 넘겨도 됩니다).
   ```
   ssh-keygen -t ed25519 -C "본인이메일@example.com"
   ```
2. 공개 키를 화면에 띄워 **전체를 복사**합니다.
   ```
   cat ~/.ssh/id_ed25519.pub          # macOS
   type %USERPROFILE%\.ssh\id_ed25519.pub   # Windows(명령 프롬프트)
   ```
3. GitHub 계정 **Settings → SSH and GPG keys → New SSH key** 에 붙여넣고 저장합니다.
4. 저장소 주소를 SSH 형식으로 바꿉니다.
   ```
   git remote set-url origin git@github.com:내아이디/저장소이름.git
   ```

> **`.pub`이 붙은 쪽만 올립니다.** 확장자 없는 `id_ed25519`는 **개인 키**이므로 절대
> 남에게 보내거나 저장소에 올리면 안 됩니다.

**잘 됐는지 확인** — 아무 파일이나 고쳐 커밋하고 push했을 때 거부되지 않으면 된 것입니다.
GitHub 저장소 페이지를 새로고침해 방금 커밋이 보이면 확실합니다.

---

## 2. 증상으로 찾기

내가 본 화면이나 오류 메시지를 아래 표에서 찾은 뒤, 해당 번호의 항목으로 내려가세요.
오류 메시지 전문을 `Ctrl+F`로 검색해도 됩니다.

### 내 증상 찾기

| 증상 | 항목 |
|---|---|
| 아무 메시지도 안 오는데 오류도 없음 | 1 |
| 알림이 안 왔는데 왜 안 왔는지 모르겠음 | **2 (해석표)** |
| `❌ [코드] 네이버 현재가 조회 실패` | 3 |
| 그래프에 점이 하나뿐임 | 4 |
| 그래프 안의 한글이 네모(□)로 깨짐 — **내 컴퓨터에서 연습할 때(5장)** | **4-1** |
| 폰트를 지정했는데도 그래프 한글이 계속 깨짐 | **4-1** |
| 봇 username이 이미 쓰이고 있다고 거절됨 | 5 |
| `telebot.py` 결과가 비어 있음 (`"result": []`) | 6 |
| `telebot.py` 실행했더니 `<Response [404]>` / `[401]` 만 뜸 | **6-1** |
| 토큰이 `None`이라고 나옴 / `.env` 값이 안 읽힘 | 7 |
| 토큰을 제대로 넣은 것 같은데 인증이 계속 실패함 (둥근 따옴표) | **7** |
| `❌ 'watchlist.csv' 파일이 존재하지 않습니다` | 8 |
| 압축을 풀었더니 `watchlist.csv`가 없고 **`watchlist.example.csv`만 있음** (7장부터) | **8** |
| 바꿔 둔 관심종목이 **기본 3종목으로 되돌아옴** | **8** |
| 종목을 고쳤는데 **자동 알림은 예전 종목으로 옴** | **8** |
| 종목코드 앞자리 0이 사라짐 (`005930` → `5930`) / 관심종목을 바꾸고 싶음 | **8-1** |
| 맥 텍스트편집기로 CSV를 고쳤더니 이상해짐 (서식·대문자·둥근 따옴표) | **8-1** |
| **오류가 없는데** 종목이 하나도 조회되지 않음 / 첫 줄이 `Code,name` | **8-2** |
| `❌ CSV 파일에 'code' 컬럼이 존재하지 않습니다` | 9 |
| `UnicodeEncodeError` (Windows) | 10 |
| `python`을 찾을 수 없다고 나옴 | 11 |
| `ModuleNotFoundError: No module named ...` (주로 8장 `libsql_client`) | **11-1** |
| 분명히 설치했는데 없다고 나옴 / 설치는 성공했다고 나왔음 | **11-1** |
| `❌ Turso DB에서 종가 히스토리를 읽지 못했습니다` / 저장 실패 | 12 |
| DB 연결이 오류도 없이 멈춰 있음 | 13 |
| 스케줄 시각이 지났는데 실행되지 않음 | 14 |
| 실행은 됐는데 빨간 ✗ | 15 |
| Streamlit에서 앱을 만들려는데 **내 저장소가 목록에 안 보임** | 16 |
| Streamlit 배포 시 저장소를 못 찾음 (404 / `This repository does not exist`) | 16 |
| Streamlit에서 Deploy 버튼이 활성화되지 않음 | 16 |
| Streamlit Secrets 화면에서 `Save` 버튼이 눌리지 않음 | [12장 세팅](#12장--streamlit-대시보드-배포) |
| Streamlit Secrets를 넣었는데 반영이 안 됨 / 문구가 `종가 히스토리 누적 전` | 17 |
| 대시보드 한글이 네모(□)로 깨짐 / 대시보드에서만 조회 실패 | 17 |
| `.env`나 토큰을 실수로 GitHub에 올림 | **18 (급함)** |
| 화면 공유·녹화·스크린샷에 토큰이 찍힘 | **18 (급함)** |
| 편집기에서 `.env`를 선택해 둔 채로 AI 도구를 씀 | **18 (급함)** |
| 대시보드를 실행하니 터미널이 멈춘 것 같음 | 21 |
| 코드를 고쳤는데 대시보드 화면이 안 바뀜 | 21 |
| `Port 8501 is already in use` | 21 |
| `/notify`를 눌렀는데 아무 반응이 없음 | 20 |
| 여러 번 눌렀는데 알림이 한 번만 옴 | 20 |
| 로그에 `🔍 새 텔레그램 메시지가 없습니다.` | 20 |
| 학기가 끝난 뒤 정리하는 방법 | 19 |
| 소스 제어 패널에 `Publish to GitHub` 버튼이 없음 / `Manage Workspace Trust`만 보임 | **22** |
| 소스 제어 패널에 `Download Git` / `Install Git` 안내가 뜸 | **22** |
| `Commit`을 눌렀더니 `COMMIT_EDITMSG`가 열리고 패널이 먹통 | **23** |
| 저장소 이름이 하이픈 범벅(`----------`)이 됨 | **24** |
| `Publish to GitHub`을 눌러도 아무 일 없이 처음 화면으로 돌아감 | **25** |
| 실수로 `Initialize Repository`를 누름 / 브랜치가 `master`로 만들어짐 | **26** |
| 로그인 창이 두 번 뜸 / `Sign in with your browser` 창이 또 나옴 (Windows) | **27** |
| `.gitignore`가 폴더에 안 보임 / `.git` 폴더가 안 보임 | [1장 세팅 7번](#1장--개발-환경과-내-저장소-만들기) |

---

### 1. 아무 메시지도 안 옵니다 (오류도 없습니다)

**원인**: 오늘이 주말이거나 공휴일이면 코드가 스스로 실행을 건너뜁니다. 주식 시장이 열리지
않는 날에는 보낼 시세가 없기 때문입니다.

**해결**: 오류가 아니니 그대로 두면 됩니다. **평일 개장일에 다시 실행**해 보세요. 실행했을 때
`📅 오늘은 KRX 개장일이 아닙니다`라는 문구가 보이면 이 경우가 맞습니다.

---

### 2. 알림이 안 왔는데 원인을 모르겠습니다 — 원인 해석표

> 이 항목은 **10장 이후**, 즉 GitHub Actions로 자동 실행을 걸어둔 다음부터 해당합니다.
> 내 컴퓨터에서 직접 실행하는 9장까지는 터미널에 뜨는 메시지를 그대로 보면 됩니다.

**"텔레그램이 안 왔다"는 사실 하나만으로는 원인을 알 수 없습니다.** 스케줄이 안 돈 것도,
실행하다 실패한 것도, 공휴일이라 보낼 게 없던 것도 겉으로는 똑같이 "조용함"으로만 보이기
때문입니다. 그래서 **GitHub Actions의 실행 결과를 반드시 같이 봐야** 구분됩니다.

내 저장소 → **Actions** 탭 → 해당 워크플로를 열고, 아래 표에서 내 상황을 찾으세요.

| 텔레그램 | Actions | 원인 |
|---|---|---|
| 안 옴 | **실행 기록이 아예 없음** | 스케줄이 통째로 건너뛰었습니다. **가장 흔한 경우**이고 내 잘못이 아닙니다 → [14번](#14-스케줄-시각이-지났는데-실행되지-않습니다) |
| 안 옴 | **빨간 ✗** | 실행은 됐는데 실패했습니다. 로그에서 `❌`로 시작하는 줄을 찾아 **네이버 조회 실패**인지 **텔레그램 전송 실패**인지 구분하세요 → [15번](#15-실행은-됐는데-빨간-로-끝났습니다) |
| 안 옴 | **초록 ✓** | 공휴일이라 보낼 게 없었던 **정상 종료**입니다 → [1번](#1-아무-메시지도-안-옵니다-오류도-없습니다) |

세 번째 줄이 "공휴일"로 확정되는 이유가 있습니다. 이 프로젝트의 코드는 **아무것도 하지 못하면
반드시 실패(빨간 ✗)로 끝나도록** 만들어져 있습니다. 그래서 초록 ✓인데 메시지가 안 왔다면 남는
답은 "보낼 게 없었다" 하나뿐입니다.

---

### 3. `❌ [코드] 네이버 현재가 조회 실패`

**원인**: 이 프로젝트는 네이버 금융의 **비공식 API**를 사용합니다. 공식적으로 공개된 것이
아니라서 일시적으로 요청이 막히거나 응답 형식이 예고 없이 바뀔 수 있습니다.

응답은 받았지만 **가격을 숫자로 읽지 못한 경우**(`❌ [코드] 가격을 숫자로 읽지 못했습니다`)도
같은 실패로 처리됩니다.

**해결**: 대부분 일시적인 문제입니다. **잠시 후 다시 실행**해 보세요. 코드는 이미 자동으로 2회
재시도하므로, 그래도 실패했다면 조금 기다렸다가 시도하는 것이 가장 확실합니다. 종목 하나만
실패한 경우 나머지 종목은 정상적으로 전송됩니다.

---

### 4. 그래프에 점이 하나뿐입니다

> **📌 7·8장이라면 여기부터 읽으세요 → [아래 「지금이 몇 장인지에 따라 뜻이 정반대입니다」](#️-지금이-몇-장인지에-따라-뜻이-정반대입니다)**
> 바로 아래 두 문단은 **DB와 `collect_daily_close.py`가 생긴 뒤(8장 이후) 이야기**입니다.
> **7장에는 그 파일도 DB도 아직 없고**, 코드가 과거 종가 자리에 빈 목록을 그대로 넘깁니다
> (`build_price_chart([], …)`). 그래서 왼쪽이 비는 것이 **당연하고 정상**입니다.

**원인**: 그래프의 왼쪽은 **과거 종가** 자리인데, 아직 쌓인 과거 종가가 없어서 비어 있는
것입니다. 알림 스크립트는 DB에 저장된 종가를 **읽기만 하고 직접 조회하지 않습니다.**

**해결**: 오류가 아닙니다. `collect_daily_close.py`가 장 마감(15:30) 후에 실행되어 그날 종가를
하나씩 쌓기 시작하면, **거래일이 지날수록 그래프 왼쪽이 채워집니다.**
(이 파일은 **8장에 처음 받습니다.** 7장에는 없습니다.)

**새로 추가한 종목도 며칠 동안은 똑같이 이 모습입니다.** 실제 운영에서도 정상적인 상태이니
그대로 두고 며칠 지켜보세요.

#### ⚠️ 지금이 몇 장인지에 따라 뜻이 정반대입니다

그래프 위에 `오늘 현재가 추이 (종가 히스토리 누적 전)`이라고 쓰여 있고 오늘 것만 보인다면,
**화면은 똑같아도 의미가 다릅니다.**

| 지금이 | 뜻 |
|---|---|
| **7·8장** | ✅ **정상입니다.** 아직 종가를 어디에도 쌓지 않았으니 그릴 것이 오늘치뿐입니다 |
| **9장 이후** | ❌ **DB를 못 읽은 것입니다.** 쌓아 둔 종가가 있는데 안 나오는 상태입니다 |

9장 이후인데 이 문구가 보인다면 확인할 곳은 이렇습니다.

- **내 컴퓨터에서 실행 중이라면** `.env`의 `TURSO_DATABASE_URL`·`TURSO_AUTH_TOKEN`
- **GitHub Actions라면** 저장소 Secrets의 같은 두 값
- **대시보드(Streamlit)라면** 앱 Secrets. 그리고 **Deploy 뒤에 Secrets를 넣거나 고쳤다면
  Reboot가 필요합니다** → [17번](#2-증상으로-찾기)

**화면에는 에러가 뜨지 않습니다.** 종가를 못 읽어도 코드가 조용히 넘어가기 때문에, 이 문구가
사실상 유일한 신호입니다.

#### 원인을 빨리 찾는 법 — 터미널(로그)을 보세요

화면에는 안 나와도 **실행한 쪽에는 원인이 그대로 찍힙니다.**

```
❌ Turso DB에서 종가 히스토리를 읽지 못했습니다: TURSO_DATABASE_URL 환경변수가 설정되지 않았습니다.
```

**어느 값이 문제인지까지 알려주므로 추측할 필요가 없습니다.** 어디를 볼지는 실행 방식에 따라
다릅니다.

| 실행 방식 | 로그를 보는 곳 |
|---|---|
| 내 컴퓨터에서 직접 실행 | 명령을 친 **그 터미널** |
| 대시보드(로컬) | `streamlit run`을 실행한 **그 터미널** |
| 대시보드(Streamlit Cloud) | 앱 화면 우측 하단 **Manage app** → 아래쪽 로그 |
| GitHub Actions | 저장소 **Actions** 탭 → 해당 실행 → 로그 |

---

### 4-1. 그래프 안의 한글이 네모(□)로 깨집니다 — 내 컴퓨터에서 연습할 때

**5장에 matplotlib을 직접 연습할 때 생기는 문제입니다.** 대시보드(배포한 앱)에서 깨지는 것은
원인도 해결도 다르니 [17번](#2-증상으로-찾기)을 보세요.

**원인**: matplotlib의 기본 폰트에는 **한글 글자가 들어 있지 않습니다.** 없는 글자는 네모(□)로
그려집니다. 오류가 아니라서 프로그램은 멀쩡히 끝나고 PNG 파일도 만들어집니다.

**해결**: 한글이 있는 폰트를 지정합니다. **운영체제마다 이름이 다릅니다.**

```python
# macOS
plt.rcParams["font.family"] = "AppleGothic"

# 윈도우
plt.rcParams["font.family"] = "Malgun Gothic"

plt.rcParams["axes.unicode_minus"] = False
```

마지막 줄은 **마이너스 기호가 깨지는 것**을 막습니다. 한글 폰트를 지정하면 마이너스 기호가
대신 깨지는 일이 있어서 같이 넣습니다.

#### ⚠️ 지정했는데도 깨진다면 — **위치**를 보세요

**이 코드는 그림을 그리기 전에 넣어야 합니다.**

| 넣은 자리 | 결과 |
|---|---|
| `ax.plot()`·`ax.annotate()` **앞** | ✅ 한글이 나옵니다 |
| 그리고 난 **뒤**, `fig.savefig()` 앞 | ❌ **그대로 깨집니다** |

`plt.rcParams`는 "앞으로 그릴 것"에 적용되는 설정이라, 이미 그려 놓은 글자에는 소급되지
않습니다. **지정했는데 안 된다는 문의는 대부분 이 경우입니다.**

#### 7장부터는 이 문제가 안 생깁니다

프로젝트 코드의 `build_price_chart()`에는 **폰트 설정이 이미 들어 있습니다.**
(그 함수가 있는 파일은 장에 따라 다릅니다 — **7·8장은 `notify_stock_price.py` 안**에 있고,
**9장부터 `stock_utils.py`로 옮겨집니다.**)
게다가 이름을 하나로 고정하지 않고 **이 컴퓨터에 실제로 깔려 있는 한글 폰트를 찾아서** 씁니다
(맑은 고딕 → 나눔고딕 → 애플고딕 순으로 확인). 그래서 윈도우와 맥에서 같은 코드가 그대로
돌아갑니다.

즉 이 항목은 **5장에 직접 그려 볼 때만** 해당합니다. 7장 이후 프로젝트 그래프에서 한글이
깨진다면 그건 폰트 설정을 안 넣어서가 아니라 **한글 폰트가 하나도 없는 환경**(주로 리눅스·배포
서버)이라는 뜻이고, 그때는 [17번](#2-증상으로-찾기)입니다.

---

### 5. 봇 username이 이미 사용 중이라고 거절됩니다

**원인**: 텔레그램 봇의 username은 **전 세계에서 하나뿐이어야** 하고, 반드시 `bot`으로 끝나야
합니다. `stock_bot`, `mystock_bot` 같은 흔한 이름은 이미 누군가 쓰고 있습니다.

**해결**: **학번이나 이름을 넣어 나만의 이름**으로 만드세요.

```
stock_2026001_bot
hong_gildong_stock_bot
```

봇 이름(표시 이름)은 겹쳐도 되고 나중에 바꿀 수도 있습니다. 겹치면 안 되는 것은 username뿐
입니다.

---

### 6. `telebot.py`를 실행했는데 결과가 비어 있습니다 (`"result": []`)

**원인**: **내가 만든 봇에게 먼저 메시지를 보내지 않아서**입니다. 토큰이 잘못된 것이 아닙니다.

`getUpdates`는 "봇이 받은 메시지 목록"을 돌려주는 기능입니다. 받은 메시지가 하나도 없으면 빈
목록(`[]`)이 나오는 것이 당연합니다. **빈 배열은 오류가 아니라 "받은 메시지가 없다"는 뜻**
입니다.

**해결**: 텔레그램 앱에서 **내 봇에게 아무 말이나 하나 보낸 뒤** 다시 실행하세요.

```
python telebot.py          # macOS: python3 telebot.py
```

이제 출력에서 `"chat":{"id": 123456789, ...}` 부분을 찾으면, 그 숫자가 `TELEGRAM_CHAT_ID`
입니다.

> 여기서 봇을 다시 만들고 토큰을 새로 발급받는 학생이 매년 나옵니다. **봇을 다시 만들지
> 마세요.** 메시지를 먼저 보내는 것만으로 해결됩니다.

---

### 6-1. `telebot.py`를 실행했더니 `<Response [404]>` / `<Response [401]>`만 뜹니다

`telebot.py`는 맨 앞에 `<Response [200]>` 같은 **응답 코드 한 줄**을 찍습니다. **이 숫자가
무엇이 잘못됐는지 알려줍니다.** 200이 아니면 그 뒤 내용이 아예 출력되지 않습니다.

| 화면에 뜬 것 | 뜻 | 할 일 |
|---|---|---|
| `<Response [200]>` + `'result': []` | **정상입니다.** 받은 메시지가 없을 뿐 | 봇에게 메시지를 먼저 보내세요 → [6번](#2-증상으로-찾기) |
| **`<Response [404]>`** | 토큰을 **아예 못 읽었습니다.** 빈 값으로 요청이 나갔습니다 | `.env` 파일 이름·위치와 **키 이름**을 확인 → [7번](#2-증상으로-찾기) |
| **`<Response [401]>`** | 토큰은 읽었는데 **틀렸습니다** | BotFather에서 토큰을 다시 확인해 `.env`에 붙여넣으세요 |

**404와 401을 헷갈리지 마세요.** 404는 *"`.env`를 못 읽었다"* 이고 401은 *"토큰 값이 틀렸다"*
입니다. **고칠 곳이 서로 다릅니다** — 404인데 토큰만 계속 다시 붙여넣으면 영원히 안 됩니다.

> `telebot.py`는 **한 번만 쓰고 버리는 스크립트**라 친절한 오류 안내가 들어 있지 않습니다.
> 응답 코드 한 줄만 보여주는 것이 이 파일의 성격입니다 — 그래서 이 표가 필요합니다.

---

### 7. 토큰이 `None`이라고 나오거나 `.env` 값이 안 읽힙니다

**원인**: 가장 흔한 원인은 **파일 이름이 `.env`가 아니라 `.env.txt`로 저장된 것**입니다.
Windows 탐색기는 기본적으로 확장자를 숨기기 때문에, 메모장으로 저장하면 `.txt`가 몰래 붙어도
화면에는 `.env`로만 보입니다. macOS도 Finder 설정에 따라 확장자가 숨겨져 있을 수 있습니다.

**해결**

1. 확장자를 **보이도록 켜고** 실제 파일 이름을 확인합니다.
   - **Windows**: 탐색기 상단 **보기 → "파일 확장명"** 체크
   - **macOS**: **Finder → 설정(⌘,) → 고급 → "모든 파일 확장자 보기"** 체크
2. `.env.txt`라면 이름을 `.env`로 바꿉니다.
3. 애초에 **`.env.example` 파일을 복사해 이름만 `.env`로 바꾸는 방법**이 가장 안전합니다. 이미
   존재하는 파일의 이름을 바꾸는 것이므로 확장자가 붙지 않습니다.

그래도 안 된다면 아래도 확인하세요.

- `.env`가 **프로젝트 폴더 맨 위**(`notify_stock_price.py`와 같은 폴더)에 있는지
- **키 이름**이 정확한지 (`TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID` — 오타·대소문자)
- **⚠️ 따옴표가 둥근 따옴표(`" "`)는 아닌지** — 아래 참고

#### ⚠️ 곧은 따옴표와 둥근 따옴표

`.env`에 값을 적을 때 **따옴표는 붙여도 되고 안 붙여도 됩니다.** 아래 셋은 **완전히 같습니다**
— 프로그램이 읽을 때 감싼 따옴표를 벗겨내기 때문입니다.

```
TELEGRAM_BOT_TOKEN=123456:ABCdef
TELEGRAM_BOT_TOKEN="123456:ABCdef"
TELEGRAM_BOT_TOKEN='123456:ABCdef'
```

**그런데 둥근 따옴표는 벗겨지지 않습니다.**

```
TELEGRAM_BOT_TOKEN=“123456:ABCdef”     ← 이렇게 하면
                                          토큰이  “123456:ABCdef”  로 읽힙니다
```

따옴표가 **값 안에 그대로 들어가서** 인증이 실패합니다. 증상이 고약합니다 — **토큰을 분명히
제대로 붙여넣었는데 안 되고**, 화면으로는 `"` 와 `“` 의 차이가 거의 안 보입니다.

**둥근 따옴표는 워드·한글·메모장의 자동 고침이 만듭니다.** 그래서 `.env`는 **VS Code**로
편집하는 것이 가장 안전하고, **애초에 따옴표를 안 쓰면 이 문제가 생기지 않습니다** —
배포해 드리는 `.env.example`이 따옴표 없이 적혀 있는 이유입니다.

> 12장 Streamlit Secrets는 반대로 **따옴표가 반드시 있어야 합니다**(형식이 다릅니다).
> 거기서도 **곧은 따옴표**여야 한다는 것은 같습니다 → [17번](#2-증상으로-찾기)
- **`Code Runner` 확장으로 실행하고 있지 않은지** — 파일 이름도 위치도 맞는데 계속 `None`이
  나온다면 이걸 의심하세요. Code Runner는 파일이 있는 폴더가 아닌 다른 위치를 기준으로 실행해서
  `.env`를 못 찾습니다. **터미널에서 `python 파일이름.py`로 직접 실행**하면 됩니다
  ([1장 세팅](#1장--개발-환경과-내-저장소-만들기)에서 설치하지 말라고 안내한 확장입니다)

---

### 8. `❌ 'watchlist.csv' 파일이 존재하지 않습니다`

**원인이 둘입니다. 먼저 폴더에 파일이 실제로 있는지 눈으로 확인하세요.**

| 폴더를 보니 | 원인 | 아래 |
|---|---|---|
| `watchlist.csv`가 **보인다** | 실행 위치(현재 폴더)가 다르다 | **8-①** |
| `watchlist.example.csv`만 있고 `watchlist.csv`는 **없다** | 7장부터 압축에 안 들어 있다 | **8-②** |

#### 8-①. 파일은 있는데 못 찾는 경우 — 실행 위치

파이썬은 "지금 있는 폴더"를 기준으로 파일을 찾기 때문에, 다른 폴더에서 실행하면 바로 옆에 있는
파일도 못 찾습니다.

**해결**: VS Code에서 **프로젝트 폴더 자체를 열었는지** 확인하세요. 파일 하나만 열어둔 상태나,
상위 폴더를 열어둔 상태에서는 이 오류가 납니다.

지금 어느 폴더에 있는지는 터미널에서 확인할 수 있습니다.

```
cd                 # Windows: 현재 폴더 경로 출력
pwd                # macOS: 현재 폴더 경로 출력
```

출력된 경로가 `watchlist.csv`가 들어 있는 폴더와 같아야 합니다.

#### 8-②. 파일 자체가 없는 경우 — **7장부터 압축에 안 들어 있습니다**

**고장이 아니라 의도한 것입니다.** 7장부터 압축에는 `watchlist.csv` 대신
**`watchlist.example.csv`**(본보기)가 들어 있습니다. 6장에 여러분이 자기 종목으로 고친
파일을 매주 덮어써 버리지 않으려고 그렇게 했습니다 — `.env`를 넣지 않고 `.env.example`만
넣는 것과 같은 방식입니다.

**해결**: `watchlist.example.csv`를 **복사해서 이름을 `watchlist.csv`로 바꾸세요.**
확장자 `.csv`는 그대로 두고 가운데 `.example`만 지웁니다. 그다음 내 종목으로 고치면 됩니다.

자세한 설명과 새로 만드는 방법은 → [1절 3~7장 세팅](#37장--세팅-없음)

#### 8-③. 반대 상황 — **바꿔 둔 관심종목이 기본 3종목으로 되돌아왔습니다**

**6장까지 사이에 생겼다면** 압축을 덮어쓰면서 되돌아간 것입니다(6장까지는 압축에
`watchlist.csv`가 들어 있습니다). 7장부터는 이 일이 구조적으로 일어나지 않습니다.

**이미 되돌아갔다면 다시 고치면 됩니다.** 다만 **되돌아간 파일을 모르고 커밋했는지**
확인하세요 — 커밋했다면 GitHub에 있는 것까지 되돌아가 있고, 10장 이후라면 **자동 알림이
기본 3종목으로 옵니다.** 고친 뒤 다시 커밋·푸시하면 정상으로 돌아옵니다.

> **내 컴퓨터에서 고쳤는데 자동 알림은 그대로인 경우**도 같은 이야기입니다. 자동 실행은
> GitHub에 올라간 파일을 읽으므로, **고친 뒤 커밋·푸시해야** 반영됩니다.

---

### 8-1. `watchlist.csv`를 고칠 때 — **엑셀로 열지 마세요**

관심종목을 바꾸려면 `watchlist.csv`를 고치게 되는데, **이 파일은 엑셀로 열지 마세요.**
**VS Code**나 **메모장**으로 고칩니다.

**왜 엑셀이 문제인가** — 엑셀은 `005930`을 **숫자로 보고 앞자리 0을 지웁니다.**
화면에 `5930`으로 뜨고, 그 상태로 저장하면 **파일 안의 값 자체가 `5930`이 됩니다.**
그러면 종목 조회가 실패합니다(네이버가 `5930`이라는 종목을 모릅니다).

| | 엑셀로 열면 | 메모장·VS Code로 열면 |
|---|---|---|
| 화면 | `5930` | `005930` |
| 저장하면 | **앞자리 0이 사라진다** | 그대로 |

**같은 파일인데 프로그램마다 다르게 보인다**는 것이 이 함정의 핵심입니다. 엑셀 화면을 믿고
"원래 이런가 보다" 하면 원인을 못 찾습니다. **의심되면 메모장으로 열어 확인하세요.**

> 5장에 `pd.read_csv(dtype={"code": str})`를 배웁니다. 그것은 **파이썬이 읽을 때** 0을
> 지키는 방법이고, 여기 이야기는 **엑셀이 저장할 때** 0을 지우는 문제입니다. 둘은 다릅니다 —
> `dtype`을 제대로 써도 파일에 이미 `5930`이 저장돼 있으면 소용이 없습니다.

**메모장으로 고칠 때 한 가지 더** — **마지막 종목 뒤에서 엔터를 한 번 눌러**
커서가 다음 빈 줄에 가 있는 상태로 저장하세요. 마지막 줄이 줄바꿈 없이 끝나면 프로그램에 따라
그 줄을 제대로 못 읽는 일이 있습니다.

```
code,name
005930,삼성전자
000660,SK하이닉스
005380,현대자동차
          ← 커서가 여기(빈 줄)에 있어야 합니다
```

배포해 드리는 `watchlist.csv`는 이미 이 상태로 맞춰져 있습니다. 고친 뒤에도 같게 두세요.

**맥에서 「텍스트편집기(TextEdit)」를 쓸 때는 세 가지를 끄고 시작하세요.** 윈도우 메모장과
달리 TextEdit은 **입력한 글자를 고쳐 주는 기능이 기본으로 켜져 있습니다.**

| 기능 | 무슨 일이 생기나 | 끄는 곳 |
|---|---|---|
| **서식 있는 텍스트** (기본값) | 저장하면 CSV가 아니라 **서식 파일**이 됩니다 | **포맷 → 일반 텍스트로 변환** (`⇧⌘T`) |
| **자동으로 단어 첫 글자를 대문자로** | 첫 줄을 다시 치면 `code` → **`Code`** 가 됩니다 | 편집 → 대치 |
| **스마트 따옴표** | `"` 가 둥근 따옴표 `“ ”` 로 바뀝니다 | 편집 → 대치 |

두 번째가 특히 고약합니다 — **오류가 나지 않고 종목만 조회되지 않습니다.**
왜 그런지는 바로 아래 [8-2번](#8-2-오류는-없는데-종목이-하나도-조회되지-않습니다-첫-줄이-codename-인-경우)에 있습니다.

> **가장 간단한 방법은 VS Code로 고치는 것입니다.** 수업에서 쓰는 편집기이고, 입력한 글자를
> 임의로 바꾸지 않습니다. 위 세 가지를 신경 쓸 일이 없습니다.

---

### 8-2. 오류는 없는데 종목이 하나도 조회되지 않습니다 (첫 줄이 `Code,name` 인 경우)

**증상**: `❌` 도 오류 메시지도 없습니다. 그런데 읽어온 목록이 이상하고,
알림이 `(0/3종목)` 처럼 **하나도 성공하지 못한 채** 끝납니다.

```
📋 읽어온 관심 종목 리스트: [5930, 660, 5380]     ← 앞자리 0이 없다
```

**원인은 첫 줄의 대문자 하나입니다.** `watchlist.csv` 의 첫 줄이 `code,name` 이 아니라
**`Code,name`** 이면 이렇게 됩니다.

6장 `read_watchlist()` 는 파일을 이렇게 읽습니다.

```python
watchlist_df = pd.read_csv(path, dtype={"code": str})          # ① 먼저 읽고
watchlist_df.columns = [col.strip().lower() for col in watchlist_df.columns]   # ② 그다음 소문자로
```

`dtype={"code": str}` 는 **`code` 라는 이름의 열을 찾아서** 문자열로 읽으라는 뜻입니다.
그런데 ① 시점에는 열 이름이 아직 `Code` 라서 **그런 이름의 열이 없습니다.**
**pandas 는 이럴 때 오류를 내지 않고 그냥 넘어갑니다** — 없는 열 이름을 줘도 조용합니다.
그래서 `005930` 이 평소대로 숫자로 읽혀 **앞자리 0이 사라집니다.**

| 첫 줄 | 읽어온 결과 |
|---|---|
| `code,name` | `['005930', '000660']` ✅ 문자열 |
| `Code,name` | `[5930, 660]` ❌ 숫자 |

**9번 오류가 대신 나 주지도 않습니다.** ② 에서 소문자로 바꾸고 나면 `code` 열이 분명히
있으니까요. 즉 **막아 주는 장치를 전부 지나쳐 조용히 틀린 값으로 굴러갑니다.**

**확인**: `watchlist.csv` 를 열어 **첫 줄**을 보세요. `code,name` 이어야 합니다.

**해결**: 첫 줄을 **소문자 `code,name`** 으로 고치고 저장합니다.

**어쩌다 대문자가 되나** — 직접 고칠 일이 없는 줄이라 드물지만, 맥의 텍스트편집기에서
첫 줄을 다시 입력하면 **「자동으로 단어 첫 글자를 대문자로」** 기능이 `code` 를 `Code` 로
바꿔 놓습니다. 끄는 방법은 위 [8-1번](#8-1-watchlistcsv를-고칠-때--엑셀로-열지-마세요)에 있습니다.

---

### 9. `❌ CSV 파일에 'code' 컬럼이 존재하지 않습니다`

**원인은 두 가지입니다.**

1. `watchlist.csv`의 첫 줄(열 이름)이 실제로 `code`가 아닌 경우.
2. **실행 위치가 잘못돼서, 다른 폴더에 있는 엉뚱한 `watchlist.csv`를 읽은 경우.**

**해결**: 내 `watchlist.csv`를 열어봤을 때 `code` 컬럼이 **분명히 있는데도** 이 오류가 난다면
2번을 의심하세요. [8번](#8--watchlistcsv-파일이-존재하지-않습니다)과 같은 방법으로 실행 위치를
확인하면 됩니다.

> **첫 줄이 `Code,name`(대문자 C)이면 이 오류는 나지 않습니다.** 프로그램이 열 이름을
> 소문자로 맞춰 주기 때문입니다. 대신 **오류 없이 앞자리 0이 사라진 채로** 굴러갑니다 —
> 그쪽은 [8-2번](#8-2-오류는-없는데-종목이-하나도-조회되지-않습니다-첫-줄이-codename-인-경우)입니다.

---

### 10. `UnicodeEncodeError` (Windows)

**원인**: 코드가 출력하는 이모지(`✅`, `❌` 등)를 터미널이 표현하지 못해서 생기는 **터미널
인코딩 문제**입니다. 코드가 잘못된 것이 아닙니다.

> **macOS에서는 생기지 않는 문제입니다.** Windows에서 터미널을 쓸 때만 해당합니다.

**해결**: 쓰고 있는 터미널에 따라 명령이 다릅니다. **VS Code 아래쪽 터미널은 보통
PowerShell**이고, 검은 창에 `C:\Users\...>` 만 보이면 명령 프롬프트(cmd)입니다.

PowerShell:
```
$env:PYTHONIOENCODING="utf-8"; python notify_stock_price.py
```

명령 프롬프트(cmd):
```
set PYTHONIOENCODING=utf-8 && python notify_stock_price.py
```

이 설정은 그 터미널 창에서만 유지됩니다. 창을 새로 열면 다시 넣어야 합니다.

---

### 11. `python`을 찾을 수 없다고 나옵니다

**원인**: macOS에서는 명령 이름이 `python`이 아니라 `python3`입니다.

**해결**

| | Windows | macOS |
|---|---|---|
| 파이썬 실행 | `python` | `python3` |
| 패키지 설치 | `pip` | `pip3` |

Windows인데도 못 찾는다면 Python 설치 시 **"Add Python to PATH"** 옵션을 켜지 않은 경우가
많습니다. Python을 다시 설치하면서 그 체크박스를 켜면 해결됩니다.

---

### 11-1. `ModuleNotFoundError: No module named 'libsql_client'`

```
ModuleNotFoundError: No module named 'libsql_client'
```

**"그 이름의 라이브러리를 못 찾겠다"** 는 뜻입니다. `libsql_client` 말고 `pandas`,
`matplotlib`, `streamlit` 등 어떤 이름으로도 날 수 있고, 해결 방법은 모두 같습니다.

**이 오류는 8장에 처음 나타나기 쉽습니다.** `libsql_client`는 7장까지 쓰지 않던
라이브러리라, 설치가 빠져 있어도 8장에 `collect_daily_close.py`를 실행하기 전까지는
아무 문제 없이 지나가기 때문입니다.

**원인은 둘 중 하나입니다.**

**① 1장 설치를 안 했거나 일부만 깔렸습니다.**
[1절 1장 9번](#1장--개발-환경과-내-저장소-만들기)에서 7개를 한 번에 까는 그 명령입니다.

**② 설치한 파이썬과 실행하는 파이썬이 서로 다릅니다.** (Windows에서 흔합니다)
컴퓨터에 파이썬이 여러 개 깔려 있으면 `pip`는 A에, `python`은 B를 가리킬 수 있습니다.
그러면 **설치는 분명히 성공했다고 나오는데 실행하면 없다고 나옵니다.** 가장 헷갈리는
경우입니다.

**해결**: `pip` 대신 **`python -m pip`** 로 설치하세요.

```
python -m pip install requests python-dotenv pandas matplotlib holidays streamlit libsql-client
```

macOS는 `python3 -m pip`을 씁니다.

`python -m pip`은 **"지금 실행되는 바로 그 파이썬에 설치하라"** 는 뜻이라, 위 ②번 상황이
아예 생기지 않습니다. `pip`이 어디를 가리키는지 신경 쓸 필요가 없어집니다.

> ⚠️ **설치할 때 이름과 코드에서 부르는 이름이 다릅니다.** 설치는 `libsql-client`(붙임표),
> 코드에서 `import`할 때는 `libsql_client`(밑줄)입니다. 오류 메시지에 밑줄로 나왔다고
> `pip install libsql_client`라고 치면 안 됩니다. 흔한 일이니 오류 메시지의 이름을 그대로
> 설치 명령에 붙여 넣지 마세요.

**제대로 깔렸는지 확인**

```
python -c "import libsql_client; print('설치됨')"
```

`설치됨`이 나오면 끝난 것입니다. 여전히 같은 오류가 나온다면 아래로 어느 파이썬이 쓰이는지
확인해 보세요.

```
python -c "import sys; print(sys.executable)"
```

---

### 12. `❌ Turso DB에서 종가 히스토리를 읽지 못했습니다` / 저장 실패

**원인**: `.env`의 `TURSO_DATABASE_URL` 또는 `TURSO_AUTH_TOKEN`이 없거나 잘못된 값입니다.
(GitHub Actions에서 실행 중이라면 Secrets에 이 두 값이 등록돼 있는지 확인하세요.)

**해결**: [1절 8장](#8장--turso-db-만들기-웹-콘솔)를 다시 따라 하며 두 값을 확인하세요. 값을
복사할 때 앞뒤에 공백이나 줄바꿈이 섞이는 경우도 흔합니다.

**이 오류가 나도 현재가 메시지는 정상적으로 전송됩니다.** 스크립트가 통째로 멈추지 않고,
과거 종가 히스토리와 추이 그래프만 비어 있게 됩니다. 즉 **알림은 오는데 그래프에 점이
하나뿐인 상태**([4번](#4-그래프에-점이-하나뿐입니다))가 됩니다.

---

### 13. DB 연결이 오류도 없이 멈춰 있습니다

**원인**: Turso 주소가 `libsql://`로 시작하면 라이브러리가 **WebSocket**이라는 방식으로
연결하는데, 학교나 회사 네트워크의 방화벽이 이 방식을 막는 경우가 있습니다. 이때는 오류
메시지도 없이 그냥 **무한정 기다리기만** 합니다. 오류가 안 나기 때문에 오히려 원인을 찾기
어려운 종류의 문제입니다.

**해결**: **이 프로젝트의 코드는 접속 직전에 주소를 `https://`로 바꿔서 연결하므로 여러분은 이
문제를 겪지 않습니다.** `.env`에는 Turso가 준 주소를 그대로 넣으면 되고, 변환은 코드가 알아서
합니다.

이 항목을 넣어둔 이유는, `stock_utils.py`에 **왜 주소를 바꾸는 코드가 들어 있는지** 알아두라는
것입니다. 이유 없이 들어간 코드가 아니라 **실제로 겪은 문제를 막기 위한 장치**입니다.

---

### 14. 스케줄 시각이 지났는데 실행되지 않습니다

**원인**: **정상입니다. 내 잘못도, cron 설정이 틀린 것도 아닙니다.**

GitHub Actions의 예약 실행(`schedule`)은 "정확히 그 시각에 실행한다"는 보장이 아니라 **"여유가
되면 해준다"** 수준의 기능입니다. 요청이 몰리면 몇 시간씩 밀리거나 그 회차를 **통째로 건너뛰기도
합니다.** 실제로 강사 저장소에서도 하루 3회 중 1회만 발동하고 그마저 4~8시간 늦은 기록이
있습니다.

**해결**: cron 문법을 붙잡고 몇 시간씩 고민하지 마세요. 지금 당장 결과를 확인하고 싶다면
**Actions 탭 → 워크플로 선택 → Run workflow** 버튼으로 직접 실행하면 즉시 돕니다. 실습 검증도
이 방법으로 합니다.

---

### 15. 실행은 됐는데 빨간 ✗로 끝났습니다

**원인**: 실행은 시작됐지만 도중에 실패한 경우입니다.

**해결**: Actions 탭에서 그 실행을 클릭해 로그를 열고, **`❌`로 시작하는 줄**을 찾으세요. 이
프로젝트의 코드는 실패할 때 반드시 `❌`로 시작하는 설명을 남깁니다. 그 문구를 이 문서에서
검색하면 해당 항목으로 갈 수 있습니다.

가장 흔한 원인은 **Secrets가 등록되지 않았거나 이름에 오타가 있는 것**입니다. 이름은 대소문자
까지 정확히 일치해야 합니다.

```
TELEGRAM_BOT_TOKEN
TELEGRAM_CHAT_ID
TURSO_DATABASE_URL
TURSO_AUTH_TOKEN
```

11장에 실습으로 Secrets를 지웠다면 **복구를 잊지 않았는지** 먼저 확인하세요.

---

### 16. Streamlit에서 내 저장소가 목록에 안 보이거나, 배포 시 404가 납니다

**증상**은 이렇게 나타납니다. 원인은 하나입니다.

- Repository 드롭다운에 **public 저장소만** 나오고, 내 저장소는 목록에 **아예 없다.**
- 이름을 직접 입력하면 **`This repository does not exist`** 라고 뜬다.
- 나머지 칸을 다 채워도 **Deploy 버튼이 활성화되지 않아** 더 진행할 수 없다.

**원인**: 내 저장소가 **private**이기 때문입니다. Streamlit은 private 저장소를 볼 권한이 없으면
그 저장소를 **아예 없는 것처럼** 취급합니다. 저장소 이름을 잘못 적은 것도, 업로드가 안 된 것도
아닙니다.

> **이 항목에서 가장 헷갈리는 지점**: 화면은 *"권한이 없습니다"* 가 아니라 **"저장소가
> 존재하지 않습니다"** 라고 말합니다. 그래서 대부분 이름 오타나 업로드 실패를 의심하며
> 한참 헤맵니다. **이 메시지를 봤다면 이름부터 다시 보지 말고 권한을 먼저 확인하세요.**

**해결**: 둘 중 하나를 선택하세요.

**(1) Streamlit에 내 저장소를 볼 권한을 준다**

승인 버튼은 **배포 화면이 아니라 계정 설정에 있습니다.** 배포 화면에서 아무리 찾아도 안내
링크가 없으니, 아래 경로로 가세요.

1. 화면의 **프로필 아이콘** → **Settings**
2. **Linked accounts**(또는 Source control) 항목에서 GitHub 옆 **`Connect`**
3. GitHub 인증 화면으로 넘어가면 요청하는 권한을 확인하고 **승인(Authorize)** 합니다.
4. **배포 화면으로 돌아와 페이지를 새로고침**합니다. ← 이걸 빼먹으면 승인을 했는데도 목록에
   여전히 안 보입니다. 이전 탭을 그대로 쓰면 갱신되지 않습니다.
5. 그래도 안 보이면 Streamlit에서 **로그아웃한 뒤 GitHub 계정으로 다시 로그인**해 보세요.

> 화면 문구나 메뉴 위치는 서비스가 바뀌면 달라질 수 있습니다. 핵심은 **"배포 화면이 아니라
> 계정 설정에서 GitHub 연결 권한을 다시 준다"** 는 것이니, 같은 뜻의 항목을 찾으면 됩니다.

**(2) 저장소를 public으로 전환한다** — 더 간단합니다. 저장소 **Settings** → 페이지 맨 아래에서
바꿀 수 있습니다.

> public으로 바꾼다면 `.gitignore`에 `.env`가 들어 있는지 **반드시** 다시 확인하세요.
> 실수로 올라간 토큰은 누구나 볼 수 있게 됩니다 → [18번](#18-토큰을-실수로-노출했을-때)

---

### 17. 대시보드에 Secrets가 반영되지 않거나, 한글이 네모(□)로 깨지거나, 대시보드에서만 현재가 조회에 실패합니다

**원인**: 셋 다 **배포된 서버의 상태** 문제입니다. 코드도 설정도 맞는데 서버가 예전 상태로
돌고 있어서 생깁니다.

- **Secrets를 넣었는데 반영이 안 됨**: Deploy를 먼저 하고 나중에 Secrets를 넣거나 고친
  경우입니다. **가장 흔합니다.**
- **한글이 네모로 깨짐**: 그래프에 쓸 한글 폰트가 서버에 설치되지 않은 상태입니다. 저장소의
  `packages.txt`가 폰트를 설치하도록 돼 있지만, 앱을 이미 띄운 뒤에는 그 설치 단계가 다시 돌지
  않는 경우가 있습니다. ⚠️ **내 컴퓨터에서 연습하다가 깨지는 것은 원인이 다릅니다** →
  [4-1번](#2-증상으로-찾기)
- **대시보드에서만 현재가 조회 실패**: 내 컴퓨터에서는 잘 되는데 대시보드에서만 실패한다면,
  배포 서버에서 네이버로 나가는 연결이 막힌 상태일 수 있습니다.

**해결**: 세 경우 모두 **앱을 완전히 재시작**하면 해결되는 경우가 많습니다.

**Manage app → ⋮(점 3개) 메뉴 → Reboot app**

재시작에는 몇 분이 걸립니다. `packages.txt`나 `requirements.txt`를 고쳤는데 반영이 안 될 때도
이 방법을 먼저 시도하세요.

#### Secrets를 고쳤는데 왜 새로고침만으로는 안 되나요?

Streamlit 앱은 화면과 상호작용할 때마다 `dashboard.py`를 **처음부터 다시 실행**합니다. 그래서
새로고침하면 반영될 것 같지만, **이미 불러온(import) 파일은 다시 읽지 않습니다.**

이 프로젝트에서 환경변수를 읽는 곳은 `stock_utils.py` 맨 위입니다.

```python
TURSO_DATABASE_URL = os.environ.get("TURSO_DATABASE_URL")
```

이 줄은 `stock_utils.py`가 **맨 처음 불러와질 때 딱 한 번** 실행됩니다. 그러니 순서가 이렇게
됩니다.

```
① Deploy       → 앱 시작. Secrets가 없으니 값은 비어 있음 → 그대로 고정
② Secrets 저장 → 화면은 다시 실행되지만
③ stock_utils  → 이미 불러온 파일이라 다시 읽지 않음 → 값은 여전히 비어 있음
```

**앱 프로세스를 새로 띄워야만(Reboot) 이 줄이 다시 실행됩니다.**

#### 이 증상을 화면에서 알아보는 법

Turso Secrets가 반영되지 않으면 **오류 화면이 뜨지 않습니다.** 과거 종가를 못 읽어도 코드가
조용히 넘어가고 대시보드는 멀쩡하게 그려집니다. 구별하는 신호는 두 가지입니다.

| | 정상 | Secrets가 반영되지 않음 |
|---|---|---|
| 제목 아래 문구 | `최근 N일 종가 + 현재가 추이` | **`현재가 (종가 히스토리 누적 전)`** |
| 그래프 x축 | 종가 날짜(`07-31` 등)가 보임 | **`현재` 만 보임** |

확실히 하려면 **Manage app 아래쪽 로그**를 보세요. 실패했다면 이런 줄이 남아 있습니다.

```
❌ Turso DB에서 종가 히스토리를 읽지 못했습니다: ...
```

**"에러가 안 났다"가 "정상이다"를 뜻하지 않습니다.** 검증은 **보여야 할 것이 보이는가**로
하세요 → [18번](#18-토큰을-실수로-노출했을-때)에도 같은 이야기가 나옵니다.

---

### 18. 토큰을 실수로 노출했을 때

**증상**: `.env`를 GitHub에 올렸다 / 화면 공유·녹화 중에 토큰이 찍혔다 / 제출한 스크린샷에
같이 나왔다 / 편집기에서 `.env`를 선택해 둔 채로 AI 도구를 썼다.

**흔한 일이고, 조치도 이미 정해져 있습니다.** 이 항목의 목적은 겁주는 것이 아니라 알아챈 그
자리에서 **몇 분 안에 정리하고 넘어가게** 하는 것입니다. 숨기는 것이 가장 나쁜 대응입니다.

**원인**: 토큰은 **비밀번호와 같습니다.** 아이디를 따로 묻지 않기 때문에, 그 값을 아는 사람은
누구나 내 DB를 읽고 쓸 수 있고 내 봇으로 메시지를 보낼 수 있습니다. 저장소가 public이면
인터넷의 누구나 볼 수 있으므로 더 급합니다.

**노출되는 경로는 생각보다 많습니다.** 아래는 전부 실제로 일어나는 것들입니다.

- **`.gitignore` 없이 `.env`를 커밋했다.** 여기서 중요한 것은, **커밋을 지우거나 되돌려도
  GitHub 이력에는 그 값이 남는다**는 점입니다. 파일을 지우는 것으로는 끝나지 않고, 이력을
  손보는 것보다 **토큰을 바꾸는 쪽이 훨씬 확실합니다.**
- **화면 공유·녹화 중에 `.env`나 Secrets 입력창이 찍혔다.**
- **편집기에서 파일 일부를 선택해 둔 채로 AI 도구를 썼다.** 요즘 도구는 **내가 선택해 놓은
  영역을 자동으로 함께 읽습니다.** 붙여넣지 않아도, 화면에 출력하지 않아도 전달될 수 있다는
  뜻입니다. **2026-07-31에 강사 본인 저장소에서 실제로 이렇게 노출됐고**, 그래서 토큰을
  교체했습니다. `.env`를 열어 볼 일이 있으면 AI 도구를 쓰기 전에 **그 탭을 닫거나 선택을
  해제**하세요.
- **과제로 올린 스크린샷에 토큰이 같이 찍혔다.** 이 수업은 매주 이미지를 제출하므로 특히
  조심해야 합니다. **올리기 전에 사진을 한 번 확대해서 확인**하세요. 터미널 로그, `.env` 편집
  화면, Secrets 입력 화면이 뒤에 걸려 있는 경우가 많습니다.

#### 해결 — "지우는 것"이 아니라 "바꾸는 것"입니다

노출된 값을 화면이나 파일에서 치우는 것은 해결이 아닙니다. **그 값 자체를 못 쓰게 만들고 새
값으로 갈아끼우는 것**만이 확실한 조치입니다.

**① 먼저 시간을 고릅니다.** Turso에서 토큰을 무효화하면 **그 DB의 기존 토큰이 한꺼번에 전부
죽습니다.** 그래서 무효화한 순간부터 새 값을 다 넣을 때까지 **아무것도 동작하지 않는 구간**이
생깁니다(대개 5~10분). 자동 실행이 예정된 시각(평일 낮)은 피하고 **저녁이나 주말**에 하세요.
강사도 이번에 금요일 밤에 했고, 다음 예정 작업이 월요일 오전이라 아무 영향이 없었습니다.

**② 무효화하고 새로 발급받습니다.** 순서를 바꾸면 안 됩니다 — 새로 만들어 둔 토큰도 무효화에
같이 휩쓸리기 때문에, **무효화 → 발급** 순서여야 합니다.

- **Turso**: https://turso.tech 웹 콘솔 → 해당 DB → 기존 토큰을 무효화(Invalidate/Revoke)한 뒤
  **Create Token**으로 새로 발급. 화면 문구나 버튼 위치는 서비스가 바뀌면 달라질 수 있으니
  같은 뜻의 항목을 찾아 진행하세요.
- **텔레그램 봇 토큰**: `@BotFather` → `/revoke` → 해당 봇 선택 → 새 토큰 발급.
  **봇을 새로 만들 필요는 없고**, `TELEGRAM_CHAT_ID`도 그대로입니다.

> **URL은 바뀌지 않습니다.** `TURSO_DATABASE_URL`은 그대로 두고 **`TURSO_AUTH_TOKEN`만**
> 갈아끼웁니다.

**③ 지금까지 등록해 둔 곳을 전부 바꿉니다.** 한 곳이라도 빠뜨리면 **그 경로만 조용히 죽습니다.**
장에 따라 등록해 둔 곳의 개수가 다르니, 아래 표에서 **내가 이미 만든 것만** 챙기면 됩니다.

| | 어디에 | 언제 생겼나 | 방법 |
|---|---|---|---|
| ① | 내 컴퓨터의 `.env` | 2장부터 (모두 해당) | 값을 새 토큰으로 바꿔 저장 |
| ② | GitHub 저장소 Secrets | 10장부터 | Settings → Secrets and variables → Actions → 해당 Secret **Update** |
| ③ | Streamlit 앱 Secrets | 12장부터 | share.streamlit.io → 내 앱 → Settings → Secrets → 해당 줄 교체 후 저장 (앱이 알아서 재시작됩니다) |

즉 **9장까지는 ①만**, **10~11장은 ①②**, **12장부터는 ①②③** 입니다.

#### 제대로 바뀌었는지 확인하는 방법

만든 곳마다 따로 확인해야 합니다. 한 곳이 되면 다른 곳도 될 것이라고 넘겨짚지 마세요.

1. **내 컴퓨터**: `python collect_daily_close.py`를 실행해 `✅ … 종가 … 기록`이 보이면 토큰
   자체는 정상입니다. **이미 저장된 값을 같은 값으로 덮어쓰는 것뿐이라 데이터가 망가지지
   않습니다.** (macOS: `python3`)
2. **GitHub Actions**: Actions 탭 → 해당 워크플로 → **Run workflow**로 직접 실행 → 초록 ✓.
3. **Streamlit 대시보드**: 배포된 주소를 열어 **그래프에 과거 종가가 나오는지** 봅니다.

**⚠️ 가장 중요한 함정 — Streamlit은 실패해도 에러가 안 납니다.** 대시보드는 DB를 못 읽어도
**빨간 에러 화면 없이** 오늘 하루 흐름만으로 그래프를 그립니다. 겉보기에는 멀쩡하고 데이터만
없는 상태라, "에러가 없으니 됐다"고 넘기면 잘못 넣은 토큰을 며칠 뒤에야 발견하게 됩니다.
**그래프 왼쪽(과거 종가 자리)이 비어 있으면 ③이 제대로 안 들어간 것**입니다
→ [4번](#4-그래프에-점이-하나뿐입니다)과 겉모습이 같습니다.

#### 마지막으로

- `.gitignore`에 `.env`가 들어 있는지 확인합니다. 같은 일이 반복되지 않게 하는 단계입니다.
- **저장소가 public이라면 더 급합니다.** 누구나 볼 수 있는 상태이므로 위 ②를 먼저 하고 나머지를
  처리하세요.

> 혼나지 않을까 걱정해서 숨기는 것이 가장 나쁜 대응입니다. 누구나 하는 실수이고, **바꿔 끼우면
> 끝나는 문제**입니다.

---

### 19. 학기가 끝난 뒤 정리하는 방법

**정리는 필수가 아니라 선택입니다.** 그대로 두어도 요금이 청구되지 않고, 관심종목
(`watchlist.csv`)만 바꿔가며 **계속 알림을 받아도 됩니다.** 아래는 "이제 안 쓰겠다"는 학생을
위한 안내입니다 — 쓰지 않는 자동 실행과 토큰을 남겨두지 않는 것이 좋은 습관이기 때문입니다.

| 정리 대상 | 방법 |
|---|---|
| 텔레그램 봇 | `@BotFather` → `/deletebot` → 해당 봇 선택 → 안내대로 봇 username 입력 |
| Turso DB | https://turso.tech 웹 콘솔에서 해당 DB 선택 → 삭제(Delete) |
| GitHub Actions | 저장소 Actions 탭 → 각 워크플로 → ⋯ 메뉴 → **Disable workflow** |
| Streamlit 앱 | https://share.streamlit.io 에서 앱 삭제 |
| GitHub 저장소 | 저장소 **Settings** → 페이지 **맨 아래** → **Delete this repository** |

**순서**: 저장소를 지우면 자동 실행도 함께 사라지므로, 저장소까지 지울 생각이면 Actions를
따로 끌 필요는 없습니다.

> ⚠️ **저장소를 지우면 그동안 쓴 코드도 같이 사라집니다.** 남겨두고 싶은 코드가 있다면 먼저
> 내 컴퓨터에 내려받아 두세요. **저장소는 남기고 자동 실행만 끄는 것**(Disable workflow)도
> 충분한 정리입니다.

---

### 20. `/notify`를 눌렀는데 반응이 없거나, 여러 번 눌렀는데 한 번만 옵니다

> 13장(수동 트리거) 내용입니다.

**먼저 확인 — 오늘이 평일 개장일인가요?** 주말·공휴일에 누르면 시세 대신 **안내 메시지 한 건**이
옵니다(*"오늘은 증시가 열리지 않는 날이라 새로 보내드릴 시세가 없습니다…"*). **정상입니다.**
쌓인 종가 추이를 보고 싶다면 그때는 **대시보드**를 여세요 — 새 시세는 텔레그램(push),
쌓인 데이터는 대시보드(pull)가 맡습니다.

**아무것도 안 온다면, 원인은 대개 하나입니다 — 트리거가 이미 소모됐습니다.**

`check_manual_trigger.py`는 텔레그램에서 메시지를 받아올 때 **"여기까지 읽었다"고 서버에
표시**합니다(`getUpdates`의 `offset`). 그래서 **한 번 누르면 한 번만 감지됩니다.**

- 실행이 한 번 지나갔다면 **그 실행이 내 버튼을 이미 가져갔습니다.** Actions에서 돌렸든
  내 컴퓨터에서 돌렸든 똑같습니다
- 로컬에서 `python check_manual_trigger.py`를 돌린 경우가 특히 헷갈립니다. 로컬에는 알림을
  보내는 다음 단계가 없어서 **`🚀 수동 트리거 감지!`만 뜨고 텔레그램은 안 옵니다**
- 트리거였든 아니든 **받아온 메시지를 전부** 확인 처리하므로, 봇에게 보낸 잡담 메시지도 함께
  사라집니다

**해결 — 누르는 것이 먼저, 실행이 나중입니다.**

```
① 텔레그램에서 /notify 입력
② Actions → Run workflow
```

한 번 누르고 Run workflow를 세 번 눌러도 **첫 번째만** 감지됩니다. 다시 확인하려면 **다시
눌러야** 합니다.

**로그로 구분하기** — Actions에서 `check` job의 로그를 보세요. 네 가지로 갈립니다.

| 로그 | 뜻 |
|---|---|
| `🚀 수동 트리거 감지!` | 정상. `notify` job이 이어서 실행됩니다 |
| `📅 트리거는 감지됐지만 오늘은 KRX 개장일이 아니라…` | 눌린 것은 인식됐고, 휴장일이라 안내 메시지만 보냈습니다 |
| `🔍 수동 트리거 메시지는 없었습니다.` | 메시지는 왔지만 `/notify`가 아니었습니다 |
| `🔍 새 텔레그램 메시지가 없습니다.` | **이미 소모됐습니다.** 다시 누르세요 |

---

### 21. 대시보드를 실행했더니 터미널이 멈춘 것 같습니다 / 고쳐도 화면이 안 바뀝니다

> 12장(대시보드) 내용입니다.

**터미널이 안 돌아오는 것은 고장이 아닙니다.**

```
python -m streamlit run dashboard.py      # Windows
python3 -m streamlit run dashboard.py     # macOS
```

이 명령은 **서버를 띄우는 것**이라, 끄기 전까지 계속 돌아갑니다. 9장까지 만든 스크립트는
"한 번 실행되고 끝"이었지만 대시보드는 다릅니다. 그래서 프롬프트(`$`, `>`)가 돌아오지 않는
것이 **정상**입니다.

#### 끄는 법

그 터미널에서 **`Ctrl+C`** 를 한 번 누르면 됩니다. 프롬프트가 돌아오면 꺼진 것입니다.

#### ⚠️ `Ctrl+C` 없이 터미널 창만 닫으면 — 가장 헷갈리는 문제가 생깁니다

창을 닫아도 **서버가 계속 살아 있는 경우가 있습니다.** 그런데 이때 아무 경고도 안 뜹니다.
다시 실행하면 Streamlit이 **비어 있는 다음 포트를 알아서 골라** 새로 띄우기 때문입니다.

```
8501  ← 처음 띄운 것 (아직 살아 있음)
8502  ← 두 번째로 띄운 것
8503  ← 세 번째…
```

그래서 이런 일이 생깁니다.

> **코드를 고치고 새로고침했는데 화면이 안 바뀝니다.**
> → 예전에 띄운 서버의 페이지를 보고 있는 것입니다. 브라우저 주소창의 포트 번호와, 방금
> 실행한 터미널에 찍힌 `Local URL`의 포트 번호가 **같은지** 확인하세요.

#### 남아 있는 서버 정리하기

먼저 무엇이 살아 있는지 봅니다.

```
lsof -i :8501        # macOS / Linux
netstat -ano | findstr :8501    # Windows
```

아무것도 안 나오면 정리된 것입니다. 나온다면 끕니다.

```
lsof -ti :8501 | xargs kill                                   # macOS / Linux
for p in 8501 8502 8503; do lsof -ti :$p | xargs kill; done    # 여러 개 쌓였을 때
```

Windows는 위 `netstat`에 나온 **맨 오른쪽 숫자(PID)** 로 끕니다.

```
taskkill /PID <숫자> /F
```

**가장 확실한 예방은 `Ctrl+C`로 끄는 습관**입니다. 창을 닫아버리면 위 과정을 매번 반복하게
됩니다.

---

### 22. 소스 제어 패널에 `Publish to GitHub` 버튼이 없습니다

**증상**: 1장에 VS Code 왼쪽 **소스 제어**(가지 모양)를 눌렀는데, 안내에 적힌 두 버튼 대신
**`Manage Workspace Trust`** 같은 것만 보입니다. `Publish to GitHub`이 아예 없습니다.

**원인**: VS Code가 이 폴더를 **제한 모드(Restricted Mode)** 로 열었습니다. 폴더를 처음 열 때
뜨는 "이 폴더의 파일 작성자를 신뢰합니까" 창에서 신뢰를 주지 않고 넘어간 경우입니다.
제한 모드에서는 git 기능이 통째로 꺼지므로 **버튼이 없는 이유가 화면에 나오지 않습니다.**

**해결**: 소스 제어 패널의 **`Manage Workspace Trust`** 를 누르고 **신뢰(Trust)** 를 고릅니다.
또는 메뉴에서 **File → 워크스페이스 신뢰 관리(Manage Workspace Trust)** 로 들어가도 됩니다.
신뢰를 주는 즉시 `Initialize Repository` · `Publish to GitHub` 두 버튼이 나타납니다.

**신뢰를 줬는데도 버튼이 없다면 — Git이 안 깔린 것입니다.** 이 경우에는 대신
**`Download Git`**·**`Install Git`** 같은 안내가 보입니다. 터미널에서 `git --version`을 실행해
확인하고, 없으면 설치한 뒤 **VS Code를 껐다가 다시 켜세요**
→ [1절 1장](#1장--개발-환경과-내-저장소-만들기) 4번

---

### 23. `Commit`을 눌렀더니 낯선 편집기가 열리고 패널이 잠겼습니다

**증상**: `Commit` 버튼을 눌렀더니 **`COMMIT_EDITMSG`** 라는 처음 보는 파일이 편집기에 열리고,
소스 제어 패널이 먹통이 된 것처럼 아무 반응이 없습니다.

**원인**: **커밋 메시지를 적지 않고 `Commit`을 눌렀기 때문**입니다. 메시지가 비어 있으면 git이
"그러면 여기에 적어라"며 편집기를 띄우고, **여러분이 그 파일을 저장하고 닫을 때까지 기다립니다.**
고장이 아니라 **기다리는 중**입니다.

**해결 — 세 동작이면 빠져나옵니다.**

1. 열린 `COMMIT_EDITMSG`의 **1번 줄**에 커밋 메시지를 적습니다 (예: `3장 코드 반영`).
   - `#`으로 시작하는 줄들은 안내문이니 그대로 두세요.
2. 저장합니다 — **Windows** `Ctrl+S` / **macOS** `⌘S`
3. 탭을 닫습니다 — **Windows** `Ctrl+W` / **macOS** `⌘W`

닫는 순간 커밋이 완료되고 소스 제어 패널이 다시 움직입니다.

**예방**: 소스 제어 패널 **맨 위 한 줄짜리 입력칸**에 메시지를 **먼저** 적고 `Commit`을
누르세요 → [1절 1장](#1장--개발-환경과-내-저장소-만들기)의 매주 반복 표 ③④

---

### 24. 저장소 이름이 하이픈 범벅이 됐습니다

**증상**: `Publish to GitHub`을 끝내고 브라우저에서 저장소를 열었더니 이름이
`----------` 이나 `git---` 처럼 하이픈만 잔뜩 붙은 이상한 이름입니다.

**원인**: **폴더 이름에 한글이 들어 있었기 때문**입니다. GitHub 저장소 이름에는 영문·숫자·
`-`·`_`·`.` 만 쓸 수 있어서, VS Code가 **쓸 수 없는 글자를 전부 `-` 하나로 바꿔** 만듭니다.
한글 열 글자면 하이픈 열 개가 됩니다.

**이걸 못 알아채는 이유가 있습니다.** 이름을 묻는 입력칸에는 **원래 한글 이름이 그대로**
보이고, 실제로 만들어질 이름은 **아래쪽 작은 회색 글씨**로만 나옵니다. 그래서 대부분 그냥
엔터를 치고 넘어갑니다.

**해결**: 이름은 나중에 바꿀 수 있습니다.

- GitHub 저장소 → **Settings** → 맨 위 **Repository name** 에 영문 이름을 넣고 **Rename**
- 이름을 바꾸면 VS Code가 보던 주소도 달라지므로, VS Code에서 폴더를 **닫았다가 다시 열어**
  `Sync Changes`가 정상 동작하는지 확인하세요.

**예방**: **메인 폴더 이름부터 영문·숫자로** 지으면 이 문제 자체가 생기지 않습니다
→ [1절 1장](#1장--개발-환경과-내-저장소-만들기) 6번

---

### 25. `Publish to GitHub`을 눌렀는데 아무 일 없이 처음 화면으로 돌아갑니다

**증상**: 이름을 입력하고 엔터를 쳤는데 잠깐 뒤 **아무 일도 안 일어난 것처럼** 원래 화면으로
돌아옵니다. 다시 눌러도 똑같습니다.

**원인**: **내 GitHub 계정에 이미 같은 이름의 저장소가 있습니다.** 대개는 **아까 이미 성공했는데
그걸 모르고 다시 누른 경우**입니다. 실패 안내가 오른쪽 아래에 잠깐만 떴다 사라져서 눈에 잘
띄지 않습니다.

**해결**: 먼저 **브라우저에서 내 GitHub 저장소 목록을 확인**하세요
(`https://github.com/내아이디?tab=repositories`).

- **이미 만들어져 있고 파일도 올라가 있다면 → 성공한 것입니다.** 더 누를 필요가 없습니다.
  VS Code 소스 제어 패널의 버튼이 `Sync Changes`로 바뀌어 있는지만 확인하세요.
- **예전에 연습으로 만들어 둔 빈 저장소와 이름이 겹친 것이라면**, 그 저장소를 지우거나
  (저장소 Settings 맨 아래 **Delete this repository**) **다른 이름으로** 다시 시도하세요.

---

### 26. `Initialize Repository`를 눌러 버렸습니다

**증상**: 1장에 소스 제어 패널의 **위쪽** 버튼(`Initialize Repository`)을 눌렀습니다. 그랬더니
**`Publish to GitHub` 버튼이 사라지고**, 대신 파일 목록과 `Commit` 버튼이 나타났습니다.

**원인**: 두 버튼은 하는 일이 다릅니다.

| 버튼 | 하는 일 |
|---|---|
| `Initialize Repository` (위) | **내 컴퓨터 안에만** 저장소를 만듭니다. GitHub과는 아무 관계가 없습니다 |
| `Publish to GitHub` (아래) | 내 컴퓨터 저장소 만들기 + **GitHub에 저장소 만들기** + 첫 커밋 + 업로드를 **한 번에** 합니다 |

위를 먼저 누르면 아래 버튼이 할 일 중 첫 단계가 이미 끝나버려서, **GitHub 쪽 작업이 통째로
건너뛰어집니다.**

**해결 — 그대로 이어서 하면 됩니다. 다시 만들 필요 없습니다.**

1. 소스 제어 패널 맨 위 입력칸에 메시지를 적고 **`Commit`** 을 누릅니다.
2. 버튼이 **`Publish Branch`** 로 바뀝니다. 그것을 누르면 이름을 묻고, 이후는 1장 안내의
   4)~7)과 같습니다 (**`public`을 클릭**하는 것도 그대로입니다).

> ⚠️ **Windows에서는 한 가지를 더 확인하세요.** 이 경로로 가면 기본 브랜치 이름이 `main`이
> 아니라 **`master`** 로 만들어질 수 있습니다. GitHub 저장소 화면 왼쪽 위의 브랜치 이름이
> `master`로 보인다면, 저장소 **Settings → Branches → Default branch** 에서 **`main`으로
> 이름을 바꾸세요.** 그대로 두면 10장 자동 실행이 **아무 오류 없이 영영 돌지 않습니다** —
> GitHub Actions는 기본 브랜치만 보기 때문입니다.

---

### 27. 로그인 창이 두 번 뜹니다

**증상**: `Publish to GitHub`을 누르고 로그인을 했는데, 저장소 이름과 `public`을 고르고 나니
**로그인 창이 또 뜹니다.** `Sign in with your browser` 라고 적혀 있습니다. 아까 로그인이
잘못된 것 같아서 멈추게 됩니다.

**원인**: **고장이 아닙니다. Windows에서는 원래 두 번 묻습니다.** 하는 일이 서로 다른 두 번이기
때문입니다.

| 순서 | 무엇을 위한 로그인인가 | 누가 묻나 |
|---|---|---|
| **첫 번째** (`Connect to GitHub`) | GitHub에 **저장소를 만들기** 위해 | VS Code |
| **두 번째** (`Sign in with your browser`) | 만든 저장소에 **파일을 올리기(push)** 위해 | Git 자체의 로그인 도우미 |

**macOS는 이 두 번째 창이 없습니다.** VS Code가 두 가지를 모두 처리해서 로그인이 한 번뿐입니다.
그래서 **같은 안내를 보고 따라 해도 Windows 학생만 창을 한 번 더 보게 됩니다.**

**해결**: **두 번째 창도 그대로 승인하면 됩니다.** 브라우저가 열리면 그 화면의 안내에 따라
승인하세요. 아이디를 다시 칠 필요는 없습니다(이미 로그인돼 있으면 확인만 누르면 됩니다).

**두 번 다 끝내면 이후 13주 동안 다시 묻지 않습니다.** 승인 결과가 **Windows 자격 증명
관리자**(macOS는 키체인)에 저장되기 때문입니다.

> **두 번째 창을 실수로 닫았다면** — 저장소는 이미 만들어져 있고 파일만 안 올라간 상태입니다.
> 소스 제어 패널의 **`Sync Changes`**(또는 `Publish Branch`)를 다시 누르면 그 창이 다시 뜹니다.
> 브라우저에서 내 저장소를 열어 파일이 있는지 먼저 확인하세요
> → [2절 25번](#25-publish-to-github을-눌렀는데-아무-일-없이-처음-화면으로-돌아갑니다)
